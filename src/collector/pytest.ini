[pytest]
; Adds this directory (src/collector) to sys.path so `import collector` (the
; package under src/collector/collector) and `import tests....` resolve when
; pytest is invoked as `python -m pytest src/collector` from the repo root.
pythonpath = .
testpaths = tests
