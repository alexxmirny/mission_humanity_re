//
// tact/tact_mission_end_finalize.cpp -- see tact_mission_end_finalize.h. Translated from the
// DISASSEMBLY, not from Ghidra's C.
//
#include "tact/tact_mission_end_finalize.h"

namespace mh::tact {

// llm_tact_mission_end_finalize_stub @0x0044da59 -- see the header's exhaustive proof. The
// original's loop reads _G_LLM_SQUAD_STATUS[i].unit_proto_id for i in [0,64) and discards every
// value; a body with no observable effect is bit-identically reproduced by having no body.
void mission_end_finalize_stub() {}

} // namespace mh::tact
