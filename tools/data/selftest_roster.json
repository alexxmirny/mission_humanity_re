{
  "_what": "fork F5I: THE SELFTEST ROSTER, AS DATA. The gate's suite list used to exist three times -- as a hand-maintained tuple in tools/run_selftests.py, as a 43-arm strcmp chain in src/mh_dll/mh_nettest/net_selftest.cpp, and as a hand-written mode list in that chain's error message -- with nothing comparing them. This file is now the single list. run_selftests.py reads `suites` and `flaky` from here, and before running anything asserts that the exe's OWN `--list-suites` output equals the set recorded here for that exe; tools/check_selftest_roster.py asserts the same set against the table parsed out of the C++ source (the lint has no build, so it reads source where the driver reads the binary).",
  "_order": "`suites` is in the order run_selftests.py runs them, which is src/mh_dll/README.md's order and is load-bearing in at least one place: `hostintest`'s first arm is an open being REFUSED over an unanswered registry, so it must run before anything binds the regions. The C++ table is kept in the same order; the assertions compare SETS, because order is this file's business and a reordered table should not red the gate.",
  "_exe": "`exe` is WHICH EXECUTABLE answers to this suite, and since fork F5I S2 there are two. net_selftest.exe (src/mh_dll/mh_nettest) is the HOSTED arm -- no MH_LIBMH_BUILD, so every promotion seam and hook row compiles in and MH_CRT() names the original binary's VAs. libmh_selftest.exe (src/mh_dll/libmh_test, generated by tools/gen_libmh_vcxproj.py) is the STANDALONE arm (MH_LIBMH_BUILD + MH_SPINE_IN_IMAGE) compiling the same 628 roster TUs as the whole program. THE SPLIT IS BY SUBJECT, NOT BY SUITE (ruling R6): a suite whose subject is hosted-only machinery stays hosted even when its subject is spine code. That is why `statetest`, `bindtest` and `hostintest` are net-side -- statetest's subject is ST2, a region moving off its STOCK VA, and bindtest asserts the hosted answer to \"where does each region live\", and hostintest's first arm is an open REFUSED over an unanswered registry, while standalone every stock base is 0 by design (mh_regions.gen.h's MH_STOCK_BASE keeps original VAs out of libmh.lib's data), so all three would assert over a table of zeros and print green. The standalone side of that question is run_gate's libref unit, which stands the registry up and opens the inbound surface for real. 17 net_selftest / 15 libmh_selftest. tools/run_selftests.py builds both and runs each row on the exe named here; tools/check_selftest_roster.py asserts, per exe, that the gate-flagged set of that exe's SUITE_TABLE is exactly the set of rows naming it -- so a suite cannot end up gated in both or in neither.",
  "_checks": "`checks` is the total of the `N checks, M failures` line(s) a suite prints on a green plain run, recorded 2026-09-16 from the exe built at fork HEAD cf777b51 by tools/prove_suite_identity.py --record. It is SUM, not last: `crttest` is two halves in one invocation (sprintf family + the five other vendored headers) and prints a line for each. null means the suite prints no such line at all -- the seven transport/orchestrator suites report by exit code and prose -- so for those the identity of a run is its exit code plus its normalised transcript and nothing here. The number is a FLOOR that grows with the domain, not a contract: it is here so that a suite which silently stops registering half its assertions is visible, since such a suite still exits 0.",
  "_seamtest": "`seamtest` is deliberately ABSENT. It is a KNOWN pre-existing failure that crashes on the baseline commit too (verified 2026-07-25 by stashing). Adding it would make the gate permanently red and train everyone to ignore the script. It is a suite, so it is in the C++ table -- but without the `gate` flag, which is why the flag exists.",
  "_why_field": "`why` is the comment run_selftests.py carried above that row, moved here verbatim-in-substance when the tuple was deleted. It is the record of WHY a suite is in the gate -- usually the defect that got it added -- and null only for the rows that predate the practice of explaining one.",
  "_exes": "WHERE EACH EXE IS BUILT FROM, added at fork F5I S4 so that a tool routing a suite does not have to carry its own copy of the two project paths. `vcxproj` is the MSBuild project that produces that exe, `dir` is its source directory, and `staged` is the basename the build bat stages into the %TEMP% staging directories. Every value in the `exe` column above must be a key here -- tools/coverage.py and tools/mutate.py assert exactly that before they build anything, so a third exe added to the roster reds them instead of silently building the wrong project.",
  "exes": {
    "net_selftest": {
      "vcxproj": "src/mh_dll/mh_nettest/mh_nettest.vcxproj",
      "dir": "src/mh_dll/mh_nettest",
      "staged": "net_selftest.exe"
    },
    "libmh_selftest": {
      "vcxproj": "src/mh_dll/libmh_test/libmh_test.vcxproj",
      "dir": "src/mh_dll/libmh_test",
      "staged": "libmh_selftest.exe"
    }
  },
  "flaky": [
    "aitest",
    "simtest",
    "statetest"
  ],
  "_flaky": "The suites that operate big graphs of heap buffers, i.e. whose failure mode includes DYING. run_selftests.py repeats these on the PLAIN pass, where a crash is the only symptom corruption produces. `simtest` joined them at SIM0 (2026-08-08): its fixture is ~4 MB of real-extent rosters and the sim writes them, so it is in exactly the population that produced both historical 0xC0000374s.",
  "suites": [
    {
      "suite": "selftest",
      "exe": "net_selftest",
      "checks": null,
      "why": null
    },
    {
      "suite": "selftest3",
      "exe": "net_selftest",
      "checks": null,
      "why": null
    },
    {
      "suite": "authtest",
      "exe": "net_selftest",
      "checks": null,
      "why": null
    },
    {
      "suite": "linktest",
      "exe": "net_selftest",
      "checks": null,
      "why": null
    },
    {
      "suite": "callstest",
      "exe": "net_selftest",
      "checks": 1238,
      "why": null
    },
    {
      "suite": "exportstest",
      "exe": "net_selftest",
      "checks": 727,
      "why": null
    },
    {
      "suite": "launchtest",
      "exe": "net_selftest",
      "checks": null,
      "why": "ADDED 2026-08-23 (TACT-PREP). It was omitted, and that omission had teeth: `launchtest` asserts MH_Launch_ParseCmdline's verb ordinals as LITERALS, so inserting a verb into the enum renumbers every later one -- and adding --tactical did exactly that on the first attempt, with nothing in the gate to report it. The test costs milliseconds and guards an append-only contract; there is no reason it was ever outside the suite."
    },
    {
      "suite": "orderstest",
      "exe": "libmh_selftest",
      "checks": 155,
      "why": null
    },
    {
      "suite": "issuetest",
      "exe": "libmh_selftest",
      "checks": 2474,
      "why": "ADDED 2026-08-27 (O4-0). The order-ISSUE oracle: the layer above `orderstest`, proving a reimplemented wrapper packs the SAME order the original packed. Its expectations come from a generated golden, so it grows with the domain rather than with hand-written constants."
    },
    {
      "suite": "interlocktest",
      "exe": "net_selftest",
      "checks": 149,
      "why": null
    },
    {
      "suite": "patchtest",
      "exe": "net_selftest",
      "checks": 101,
      "why": "ADDED 2026-09-12 (F1E). The in-memory static-patch applier and its REFUSALS -- a moved guarded byte, an image that already carries the patch, a site inside a promoted body, a cave VA that is taken. Same argument as interlocktest: each is about a write that must NOT happen, so a green rig run cannot stand in for it, and the applier's host table is injected so it touches no real image. Milliseconds."
    },
    {
      "suite": "tombstonetest",
      "exe": "net_selftest",
      "checks": 41,
      "why": "ADDED 2026-09-01 (X-TOMB). The tombstone instrument's arming DECISION -- which bodies are filled, over what extent (entry+8 for a promoted body's live redirect, whole for a dead one), and which are correctly skipped. Like interlocktest it is about things NOT happening, so a green rig run cannot stand in for it; the fill is injected, so it touches no real memory."
    },
    {
      "suite": "hostapitest",
      "exe": "net_selftest",
      "checks": 103,
      "why": "ADDED 2026-09-02 (LIB-ABI stage B). The host-callback table's BINDING CONTRACT: the version handshake (a mismatched host is refused without clobbering a working binding), the unbound-walk (a missing entry is reported BY NAME), and the trap discipline (a REQUIRED entry reached through the selftest host names itself -- the carrier for the done_when's \"no-op'ing a non-notify entry is caught by a test that names it\")."
    },
    {
      "suite": "hostintest",
      "exe": "net_selftest",
      "checks": 35,
      "why": "STAYS HOSTED (F5I R8): its first arm is an open being REFUSED over an unanswered registry, i.e. a precondition about the STOCK bind, and standalone every stock base is 0 by design (mh_regions.gen.h's MH_STOCK_BASE). The standalone inbound surface is proven instead by run_gate's libref unit, which opens it for real."
    },
    {
      "suite": "statetest",
      "exe": "net_selftest",
      "checks": 75,
      "why": null
    },
    {
      "suite": "boottest",
      "exe": "libmh_selftest",
      "checks": 44,
      "why": "LIB-BOOT: the post-cfg snapshot import path, its schema guards, the ordering refusal, and the per-block mutation arm that says no carried block is outside the comparison."
    },
    {
      "suite": "worldtest",
      "exe": "libmh_selftest",
      "checks": 30,
      "why": "LIB-WORLD: the step-0 world fixture -- the format, the refusals, and all THREE coverage arms (hash-slice mutation over the 61 determinism slices, per-block mutation over all 829 blocks, per-block content). ~8 s plain and it earns them: arm B alone re-imports a 7.7 MB blob once per block, which is the only way to say that no carried block is outside the comparison. Under ASan it is slower still and stays in anyway -- this suite mallocs and fills a 7.7 MB arena hundreds of times, which is exactly the shape the ASan pass exists for."
    },
    {
      "suite": "navtest",
      "exe": "libmh_selftest",
      "checks": 20,
      "why": "ADDED 2026-09-11 (LIB-REF). The world blob's FORMAT 2 nav trailer: the map-region decomposition carried as slot indices instead of rebuilt from the `passable` plane, because a rebuild is measurably NOT the recording's partition (2181 of 65536 tiles differ). This suite round-trips a synthetic pool and asserts the reconstruction is SLOT-EXACT -- both list ORDERS, not just membership, because the free list's order decides which node a later region_split gets. The end-to-end proof is the standalone replay, which costs a rig and a fixture; this is the half that costs neither, and it is where every one of the format's refusals is actually fired. Mutation-proven three ways (push-front relink, terrain_flags clobber, free-node neighbour translation), each turning exactly one arm red."
    },
    {
      "suite": "bindtest",
      "exe": "net_selftest",
      "checks": 108,
      "why": "ADDED 2026-09-06 (SB-BIND T1). The state ABI: the host answers where every region lives, through the same C entry a standalone host will use. Shares state_selftest.cpp's TU but is its own suite -- its subject is the BINDING, not the move. Not in FLAKY: its only arena is a 10 KB static, and the registry it touches it puts back."
    },
    {
      "suite": "aitest",
      "exe": "libmh_selftest",
      "checks": 4236,
      "why": null
    },
    {
      "suite": "simtest",
      "exe": "libmh_selftest",
      "checks": 25634,
      "why": null
    },
    {
      "suite": "tacttest",
      "exe": "libmh_selftest",
      "checks": 8334,
      "why": "ADDED 2026-08-24 (TACT-DOMAIN). The OFFLINE half of the tactical oracle: it proves all 14 tactical hash slices are actually read and that the manifest lengths match the record strides the disassembly recovered. Not in FLAKY -- its arena is ~880 KB of vectors it only reads and pokes one byte of at a time, not the mutable roster graphs that produced both 0xC0000374s."
    },
    {
      "suite": "crttest",
      "exe": "libmh_selftest",
      "checks": 143,
      "why": "ADDED 2026-09-08 (LIB-CRT). The vendored sprintf family: crt/crt_sprintf.h supplies the 12 shapes a standalone libmh cannot reach at a VA, and this says they mean the same thing. The half that earns the suite is the six NARROW sites -- they build filenames (\"init\\AI03.SCR\", \"poz3o.dat\"), nothing else in the tree compares them, and a wrong one loads a different file instead of misdrawing a pixel. Not in FLAKY: no arena at all, just local buffers."
    },
    {
      "suite": "fptest",
      "exe": "libmh_selftest",
      "checks": 79,
      "why": "ADDED 2026-09-08 (CRT-X87 step 2). mh/fp/x87.h's two helpers were inline x87 and are now C++; this keeps a verbatim copy of the assembly as the reference arm and re-proves the equality at BOTH precision settings every run. Its negative arm is load-bearing -- it requires a 53-bit intermediate to still diverge at PC=64, so a sweep that went blind fails instead of passing. CHECK COUNT 78 -> 79 at fork F5I S2 (ruling R9), and the claim changed shape with it: the T table's four PC=53-DEPENDENT rows are asserted ARM-AWARE (nonzero at PC=64 hosted, where this TU compiles /arch:SSE2; equal to the PC=53 column standalone, where libmh_test.vcxproj's project-wide /arch:IA32 -- the archive's own flag -- puts the C++ body in the same x87 registers as the __asm reference), and one NEW check was added in both arms: the blindness guard, which requires trunc_scaled_int to separate further at PC=64 than at PC=53 so that a zero in the standalone PC=64 column cannot be an un-raised control word. Falsified by hand once: removing the _PC_64 install reds it."
    },
    {
      "suite": "lockstest",
      "exe": "libmh_selftest",
      "checks": 595,
      "why": null
    },
    {
      "suite": "resynctest",
      "exe": "libmh_selftest",
      "checks": 33,
      "why": null
    },
    {
      "suite": "netsessiontest",
      "exe": "libmh_selftest",
      "checks": null,
      "why": null
    },
    {
      "suite": "libtranstest",
      "exe": "libmh_selftest",
      "checks": null,
      "why": "ADDED 2026-09-02 (LT0). The lib_trans domain oracle's expectation layer: the RNG-family golden vectors + batch-A wrapper contracts, pinned against the verified rng_next body."
    },
    {
      "suite": "watchdogtest",
      "exe": "net_selftest",
      "checks": 19,
      "why": null
    },
    {
      "suite": "desynctest",
      "exe": "net_selftest",
      "checks": 53,
      "why": "ADDED 2026-08-30 (D21). The runtime desync detector's decisions: what counts as a comparable sample, which regions the verdict drops, and the two arms that must NOT report a desync (a sample from a step we have not reached, and one whose ring entry is gone)."
    },
    {
      "suite": "queuetest",
      "exe": "net_selftest",
      "checks": 16,
      "why": "ADDED 2026-09-02 (D24). Which inbound frame a FULL transport queue may destroy. The defect it guards is silent and luck-dependent -- a peer whose main thread froze lost 15 replicated orders to the old drop-oldest policy -- so a green rig campaign cannot stand in for it."
    },
    {
      "suite": "savetest",
      "exe": "libmh_selftest",
      "checks": 192,
      "why": null
    }
  ]
}
