{
  "_hand_authored": "Hand-authored. `migration_ready.py --domain sim_resid` reads it; nothing writes it.",
  "_note": [
    "Acknowledged readiness blockers for the sim_resid domain (SIM-RESID-READY). Same bar as",
    "ai_ready_ack.json and sim_ready_ack.json: an entry must name the MECHANISM and cite the",
    "EVIDENCE. 'I could not fix it' is not a reason, and an entry that only defers the work must",
    "name the item that owns it.",
    "",
    "THE LIST IS EMPTY, AND THAT IS THE POINT. SIM-RESID-READY closed on 2026-08-31 with",
    "`migration_ready.py --domain sim_resid --check` exiting 0 over all 38 live migration functions",
    "AND their 92-callee frontier, with ZERO acks -- every one of the 34 blockers it started with",
    "was FIXED rather than waived. That matters because a green standing on acks is not the same",
    "green as one standing on fixes, and the difference is invisible in the exit code. The file",
    "exists so that (a) the domain owns the artifact its profile declares in `paths.ready_acks`,",
    "and (b) the zero is on the record rather than being the absence of a record.",
    "",
    "THE TWO THINGS THAT LOOK ACKABLE AND ARE NOT, so a later session does not 'restore' them:",
    "  * _G_LLM_TUTORIAL_RESET_SLOT_0050A678 and _G_LLM_STRAT_INJECTED_MAP_PLANET_SLOT carry",
    "    PROVENANCE names, not semantic ones -- their meaning is genuinely unresolved and the",
    "    manifest notes carry the experiment that would settle each. They are not acks: they are",
    "    named, registered and bound, so no check is being waived. The uncertainty lives in the",
    "    comment, which is where this repo puts uncertainty.",
    "  * The R7 conf:med rows (8 of them) are ADVISORY, not HARD, and R7 is not ackable anyway --",
    "    an unsettled reading is settled by reading, not by recording that it is unsettled.",
    "",
    "IF AN ACK IS EVER ADDED HERE, the negative arm must be re-run: delete it and confirm --check",
    "exits 1 and names the function it covered. With zero acks that arm was demonstrated on the",
    "equivalent mechanism instead (2026-08-31) -- removing STRAT_INJECTED_MAP_PLANET_SLOT from",
    "state_regions.json makes --check exit 1 with exactly one HARD blocker, `R8 region registered`",
    "on llm_game_start_tutorial naming that region; restoring it returns exit 0."
  ],
  "_not_for": "R7 conf:low/med is NOT ackable -- an unsettled name is settled by reading, not by recording that it is unsettled.",
  "acks": []
}
