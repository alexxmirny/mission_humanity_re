{
  "_meta": "Fork disposition for every TOP-LEVEL tools/*.py. Generated-and-gated data, not prose: tools/check_tool_dispositions.py is the lint_repo row that keeps it true, and docs/fork-inventory.md section 1 is the F0 measurement this was seeded from. class=keep means the tool belongs to machinery the fork carries (gate, lint, build, harness, libmh/mh_net, or an operator's hands); class=archive means it belongs to machinery the fork drops, WHICH IS NOT A LIVENESS CLAIM -- many archive rows are still wired at HEAD (`--wired` prints them; each is an invoker the fork cut must remove too). hand_run=true declares 'no machine invoker, a human runs this' and is checked both ways.",
  "rows": {
    "_cstrip.py": {
      "class": "keep",
      "why": "the shared comment-stripper; imported at module level by gen_libmh_calls.py and gen_libmh_rebind.py and inside coverage.py's scanners -- all keep gates the public lint runs",
      "evidence": "fork F5M S4a (2026-09-16): re-classed from archive when the archive-class withhold rule was drawn -- a keep gate IMPORTS this module, so the public tree cannot run its own lint without it. Measured by import closure over the keep set; the publish-ledger closure arm cannot see this class of edge, because it scans path-form strings and `import _cstrip` has none."
    },
    "_dllsrc.py": {
      "class": "keep",
      "why": "the DLL source trees and the one module-relative key space every scanner keys by; imported by the fork's own gates (lint_x87_asm, lint_internal_edges, check_libmh_outbound, check_movable_addresses, check_roster_caps, scan_pointer_slots, coverage, gen_va_census)",
      "evidence": "fork F5O (2026-09-16): the roster moved from mh/ to libmh/ and harness.cpp to mh_harness/, so the walk roots became three; this module is where that is stated once instead of twenty times"
    },
    "_ghidra_lease.py": {
      "class": "archive",
      "why": "Ghidra DB write lease, Jython side",
      "evidence": "F0 triage (docs/fork-inventory.md section 1): the Ghidra DB pipeline (the fork freezes the database)"
    },
    "_reimpl.py": {
      "class": "keep",
      "why": "the `how much is OURS` derivation; imported at module level by gen_libmh_calls.py, gen_hostapi_callers.py, gen_region_accessors.py and gen_libmh_rebind.py -- all keep gates the public lint runs",
      "evidence": "fork F5M S4a (2026-09-16): re-classed from archive when the archive-class withhold rule was drawn -- a keep gate IMPORTS this module, so the public tree cannot run its own lint without it. Measured by import closure over the keep set; the publish-ledger closure arm cannot see this class of edge, because it scans path-form strings and `import _cstrip` has none."
    },
    "_subsys.py": {
      "class": "keep",
      "why": "the subsystem taxonomy; imported at module level by gen_state_registry.py and gen_region_accessors.py, both keep gates the public lint runs",
      "evidence": "fork F5M S4a (2026-09-16): re-classed from archive when the archive-class withhold rule was drawn -- a keep gate IMPORTS this module, so the public tree cannot run its own lint without it. Measured by import closure over the keep set; the publish-ledger closure arm cannot see this class of edge, because it scans path-form strings and `import _cstrip` has none."
    },
    "_worklist.py": {
      "class": "archive",
      "why": "naming-pipeline batching/weighting library",
      "evidence": "F0 triage (docs/fork-inventory.md section 1): the naming/documentation pipeline (Ghidra-side, dropped with the DB)"
    },
    "abc_region_diff.py": {
      "class": "keep",
      "why": "tooling TL-GATE-D25FX: names the BYTES two UI-REC arms disagree about (R-column diff + RX rdump byte diff) and checks them against the declared A/B/C excusals -- the evidence half of tools/data/abc_excusals.json",
      "evidence": "hand-run by the operator when an A-vs-B leg goes red or an excusal row is added/refreshed (ui-testing skill, RECORDED-SESSION path); its output is committed as tools/data/abc_excusal_evidence.json, which lint_fixture_currency.py checks",
      "hand_run": true
    },
    "apply_annotations.py": {
      "class": "archive",
      "why": "declarative Ghidra rename/label/comment batch applier",
      "evidence": "F0 triage (docs/fork-inventory.md section 1): the Ghidra DB pipeline (the fork freezes the database)"
    },
    "audit_names.py": {
      "class": "archive",
      "why": "naming-pipeline: named-function revalidation worklist",
      "evidence": "F0 triage (docs/fork-inventory.md section 1): the naming/documentation pipeline (Ghidra-side, dropped with the DB)"
    },
    "bmp_to_png.py": {
      "class": "keep",
      "why": "UI-harness capture BMP->PNG converter",
      "evidence": "no machine invoker -- run by an analyst turning a harness capture into something readable",
      "hand_run": true
    },
    "bootstrap.py": {
      "class": "keep",
      "why": "environment setup checker (needs its Ghidra checks pruned)",
      "evidence": "no machine invoker -- run by a human on a fresh or relocated machine (the bootstrap skill's step 1); documented in .claude/skills/bootstrap/SKILL.md, .claude/skills/mh-toolchain/SKILL.md",
      "hand_run": true
    },
    "build_data_naming_batch.py": {
      "class": "archive",
      "why": "naming-pipeline: data-naming batch pre-build",
      "evidence": "F0 triage (docs/fork-inventory.md section 1): the naming/documentation pipeline (Ghidra-side, dropped with the DB)"
    },
    "build_public_seed.py": {
      "class": "keep",
      "why": "fork F5M S5/S6: builds the public seed -- materializes the publish set, derives the public .gitignore/.gitattributes from a per-entry disposition table, authors the one commit, and re-scans the materialized bytes for operator identity",
      "evidence": "invoker: tools/lint_repo.py (two rows: --check, --selftest). The public tree runs it too -- on the cut's output --check asserts the installed config IS the committed snapshot under tools/data/public_repo_config/"
    },
    "call_graph.py": {
      "class": "keep",
      "why": "generic static call-graph walker over the committed .asm/decomp exports -- the surviving half of the deleted derive_write_closure.py",
      "evidence": "used by gen_boot_snapshot.py, gen_notify_readback.py"
    },
    "check_arm_order.py": {
      "class": "keep",
      "why": "fork F3A: the harness arm-order baseline gate (arm-log line order is part of the refactor contract)",
      "evidence": "invoked by tools/lint_repo.py"
    },
    "check_browser_rows.py": {
      "class": "keep",
      "why": "mp:R2b: the browser_two_rows scenario's claim that a browsing client LISTED every relay-hosted lobby as its own row and that each JOIN dialled the room of the row it CLICKED (second, then first) -- read off the client's `; R2 relay directory: ... -> listed` rows and its `; R2b join: row ... room=` lines against the hosts' minted rooms, since a frame cannot show which room a join dialled.",
      "evidence": "invoked by tools/lint_repo.py (--selftest, the planted-log negatives) and as the browser_two_rows scenario's post_check in tools/test_ui.py"
    },
    "check_build_probe.py": {
      "class": "keep",
      "why": "mp:D35: the configuration-(1) (no libmh.dll) build-click rows' cross-peer post_check -- with the mh.dll seam's charge-free probe (ship default) the host logs the probe line and the pair stays IDENTICAL with the in-band desync watch clean; with [net] build_probe_free=0 (the reproduction arm) the retail pay + re-grant diverges pK_ai_econ, never heals, and the watch logs *** DESYNC",
      "evidence": "invoked by tools/test_ui.py's d35_buildclick_c1 / d35_buildclick_c1_retail TESTS entries (post_check, post_check_peers, post_check_session) and tools/lint_repo.py (--selftest)"
    },
    "check_cam_trace.py": {
      "class": "keep",
      "why": "mp:SES3/SES3c (tooling:TL-SUITE-SPLICE-CAM) -- reads the camera latches on the [input] mouse_trace line; the cam_latch scenario's post-check (two --segment verdicts off one boot) and the reader a player's stuck-scroll log is judged with",
      "evidence": "invoked by tools/test_ui.py (TESTS post_check, cam_latch); its own --selftest plants a log per outcome"
    },
    "check_cancel_task.py": {
      "class": "keep",
      "why": "mp:D28: the d28_canceltask_local UI scenario's cross-peer post_check (the ship arm is host_clicks' canceltask segment) -- with the cancel-task Yes routed as a building order (ship default) the host's log carries the routed line and the pair stays IDENTICAL; with [net] cancel_task_order=0 (the reproduction arm) the retail local call diverges `buildings` and never heals",
      "evidence": "invoked by tools/test_ui.py's d28_canceltask_local TESTS entry (post_check, post_check_peers) and tools/lint_repo.py (--selftest); its net-log reader + needles are reused by check_host_clicks.py"
    },
    "check_host_clicks.py": {
      "class": "keep",
      "why": "tooling:TL-SUITE-SPLICE-HOSTCLICK -- the host_clicks UI scenario's per-segment post_check: one match runs the U39 diplomacy, D28 cancel-task and D25 build-click host clicks, and each segment is judged on its own sim window (the `log hc <seg>` gameclock fences) -- a fresh state-hash divergence inside it, a peer missing rows, and for D28 the seam banner + routed line -- so one red segment cannot hide the other two",
      "evidence": "invoked by tools/test_ui.py's host_clicks TESTS entry (three post_check commands, post_check_peers, post_check_session) and tools/lint_repo.py (--selftest)"
    },
    "check_cheat_gate.py": {
      "class": "keep",
      "why": "mp:CH1: the ch1_cheat UI scenario's cross-peer post_check -- a single-player cheat typed on the Shift+Enter console line in a lockstep match is refused (the CH1 seam's refusal line on the typing peer), order 0xfa never reaches either peer's order buffers (harness order_log), no sim-path elimination before SESSION_END, no *** DESYNC, and a stable [netind] name if the sampler ran",
      "evidence": "invoked by tools/test_ui.py's ch1_cheat TESTS entry (post_check, post_check_peers) and tools/lint_repo.py (--selftest)"
    },
    "check_codepage_adopt.py": {
      "class": "keep",
      "why": "mp:F3c: the codepage_adopt / codepage_refused UI scenarios' cross-peer post_check -- the joiner adopted the host's codepage (both logs), or was refused AND told (host REFUSED + sent JOIN_REFUSED, client JOIN REFUSED + join_refused session close, host never ADMITTED)",
      "evidence": "invoked by tools/test_ui.py's codepage_adopt and codepage_refused TESTS entries (post_check, post_check_peers) and tools/lint_repo.py (--selftest)"
    },
    "check_const_view.py": {
      "class": "keep",
      "why": "compile-time proof: module state views reject illegal writes",
      "evidence": "invoked by tools/lint_repo.py"
    },
    "check_cspec.py": {
      "class": "archive",
      "why": "validates the Ghidra x86watcom cspec before install",
      "evidence": "F0 triage (docs/fork-inventory.md section 1): the Ghidra DB pipeline (the fork freezes the database)"
    },
    "check_data_timeout.py": {
      "class": "keep",
      "why": "mp:GS2: the gs2_data_timeout UI scenario's cross-peer post_check -- a peer whose sim is fenced (harness simstep, transport left running) is dropped by the game-level data-silence watchdog at data_timeout_ms: exactly one `; GS2: peer N data-silent for X ms > T -> dropped` line across both peers, the frozen peer never drops the healthy survivor, and the drop fires within a small margin of its own T",
      "evidence": "invoked by tools/test_ui.py's gs2_data_timeout TESTS entry (post_check, post_check_peers) and tools/lint_repo.py (--selftest)"
    },
    "check_direct_dial.py": {
      "class": "keep",
      "why": "mp:R7a: the direct_dial_with_relay_set scenario's claim -- a client with `[net] relay` set that uses *Internet server* + a typed IP connects DIRECT and never contacts the relay -- made an assertion rather than an implication, by reading the client's mh_net.log for `; R7a: client dial is DIRECT` and the ABSENCE of any relay contact, and the relay's own counters line for peers_registered=0.",
      "evidence": "invoked by tools/lint_repo.py (--selftest, the planted-log negatives) and as the direct_dial_with_relay_set scenario's post_check in tools/test_ui.py"
    },
    "check_dispatch_closure.py": {
      "class": "archive",
      "why": "SIM1-DISPATCH closure/ownership completeness check",
      "evidence": "F0 triage (docs/fork-inventory.md section 1): the migration / promotion / ledger machinery the fork drops"
    },
    "check_el2_mother.py": {
      "class": "keep",
      "why": "mp:EL2: the el2_mother_deploy UI scenario's cross-peer post_check -- in the shipped configuration (no libmh.dll, udp) the host's and then the JOINER's mothership deploys each write the unit-teardown presence line (`ua=0 ba=1`, caller 0x00487f41) on BOTH peers at the same game clock, the D21 in-band desync watch agrees past the joiner's landing, and the joiner's ESC-quit writes the field's `mode=1 gate=eliminated ... caller=0x0049ddad sf=0x1b` line (llm_net_player_remove's self-removal, U17) followed by outcome 4 -- the eb1c9f9d 'eliminated with no building at 6 s' report read correctly: an unlanded ship and a quit",
      "evidence": "invoked by tools/test_ui.py's el2_mother_deploy TESTS entry (post_check, post_check_peers, post_check_session) and tools/lint_repo.py (--selftest)"
    },
    "check_fork_d5_hooks.py": {
      "class": "keep",
      "why": "fork D5: harness-owned code arms only through the named hook points, never the raw inline-detour primitives",
      "evidence": "invoked by src/mh_dll/mh_harness/mh_harness.vcxproj, tools/lint_repo.py"
    },
    "check_fork_d8.py": {
      "class": "keep",
      "why": "fork D8: the deferred-effect machinery stays deleted (zero-survivor scan)",
      "evidence": "invoked by tools/lint_repo.py"
    },
    "check_fork_f2_drop.py": {
      "class": "keep",
      "why": "fork F2: the per-row configuration vocabulary stays deleted (zero-survivor scan over src/ + tools/)",
      "evidence": "invoked by tools/lint_repo.py"
    },
    "check_ghost_slot.py": {
      "class": "keep",
      "why": "mp:GS1(b): the ghost_exit_rejoin UI scenario's cross-peer post_check -- proves no lockstep slot is pinned at the 10000 ms initial horizon from each peer's own mh_lockstep.log: the committed horizon (and clock) reach past PASS_MS on both peers (a slot pinned ANYWHERE holds committed at its own value forever, so this cannot be fooled by a slot the log's two printed columns do not cover), and the REMOTE peer's printed slot (a side's own slot is never written by that side, by design, so it is excluded) is not still exactly the initial 10000 ms advertisement once the match has run well past it",
      "evidence": "invoked by tools/test_ui.py's ghost_exit_rejoin TESTS entry (post_check, post_check_peers) and tools/lint_repo.py (--selftest)"
    },
    "check_graceful_quit.py": {
      "class": "keep",
      "why": "mp:U19: the clean ESC-quit scenario's post_check -- the quitter announced its own removal over a LIVE link, the survivor learned of it from the lockstep dispatch rather than from the socket dying, and both peers ended the match on the same game clock",
      "evidence": "invoked by tools/test_ui.py's graceful_quit TESTS entry (post_check); its own --selftest plants a log pair per outcome"
    },
    "check_inmem_composition.py": {
      "class": "keep",
      "why": "fork: the in-memory patch composition matches the manifest",
      "evidence": "invoked by tools/lint_repo.py"
    },
    "check_inmem_patch_parity.py": {
      "class": "keep",
      "why": "fork: in-memory patching reaches byte parity with the static mhpatch output on a real stock-exe lane",
      "evidence": "invoked by tools/lint_repo.py, tools/run_gate.py"
    },
    "check_instrument_wiring.py": {
      "class": "keep",
      "why": "fork: every declared instrument is wired to a real arm site",
      "evidence": "invoked by tools/lint_repo.py"
    },
    "check_libmh_outbound.py": {
      "class": "keep",
      "why": "libmh layering: no outbound call leaves libmh except through the host-callback ABI",
      "evidence": "invoked by src/mh_dll/libmh_dll/libmh_dll.vcxproj, tools/lint_repo.py"
    },
    "check_lobby_ping.py": {
      "class": "keep",
      "why": "mp:L1b: the lobby slot-row panel's per-slot ping cell's NUMBERS, read off the `; [lobbyping]` lines in mh_net.log (src/mh_dll/mh/ui/lobby_ping.cpp, ticked from the lobby's own per-frame dispatch, not the present hook). The match_launch_net capture baseline proves the cell is drawn (with the digits masked -- they differ every run); this proves at least one connected peer's row carried a real measurement rather than a permanent `n/a`.",
      "evidence": "invoked by tools/lint_repo.py (--selftest, the planted-log negatives) and tools/test_ui.py's match_launch_net TESTS entry (post_check, post_check_peers, post_check_session) -- wired 2026-09-22 wave-3 lane C"
    },
    "check_lookahead_gate.py": {
      "class": "keep",
      "hand_run": true,
      "why": "mp:P10: says from a pacing run's own mh_net.log WHICH mh.dll it ran -- re-evaluates the grow gate's condition (tail95 < 0.5*sim_step AND slack - owd > 0.5*sim_step AND owd >= 0) off the numbers the `; [adaptive]` line printed, and a window that grew under it is proof the gate was absent. Nothing else a run reports can tell the two builds apart: mp_pacing_report's `armed` line reads the INI, which is identical either way.",
      "evidence": "hand-run beside mp_pacing_report.py on a --determinism run dir. Written 2026-09-22 after a source restore without a rebuild silently ran two rig arms on the previous binary (docs/dead-ends.md G283); re-running it over the whole run set separated the builds cleanly (6 pre-fix dirs GATE ABSENT, every post-fix dir GATE PRESENT or NOT EXERCISED)."
    },
    "check_map_redirect.py": {
      "class": "keep",
      "why": "mp:X2a -- the open-redirect witnessed for REAL on the two independent rig VMs: reuses check_map_transfer's --expect-gate wholesale for the claim/gate/download/unchanged-file clauses, then additionally reads the `; [map] client resolve stored mh_dl\\... -- redirecting ... to it` line, which a local lane's shared-Maps symlink can never make load-bearing (its base file IS the host's content) but X2a's genuinely mutated client copy does",
      "evidence": "invoked by tools/test_ui.py's run_x2a_map_variant (--x2a-map-variant) and tools/lint_repo.py (--selftest)"
    },
    "check_map_transfer.py": {
      "class": "keep",
      "why": "mp:X2 -- the two peers agreed about a map they disagreed about: the host claimed content, the joiner stored it under the content-addressed name outside the map directory, its own file is byte-unchanged, and the host held Start meanwhile. Needs BOTH peers logs, so a single-lane checker could only ever report half the claim",
      "evidence": "invoked by tools/lint_repo.py (--selftest, the planted negative cases) and by tools/test_ui.py (map_absent, map_conflict, map_have post_check_peers)"
    },
    "check_module_bind.py": {
      "class": "keep",
      "why": "fork: each module binds the state regions its contract declares",
      "evidence": "invoked by tools/lint_repo.py, tools/run_gate.py"
    },
    "check_movable_addresses.py": {
      "class": "keep",
      "why": "no code spells a MOVABLE region's address as a constant",
      "evidence": "invoked by tools/lint_repo.py"
    },
    "check_net_indicator.py": {
      "class": "keep",
      "why": "mp:L1: the player-visible connection indicator's NUMBERS, read off the `; [netind]` lines in mh_net.log. The suite's capture baseline proves the element is drawn in the right place with the right labels and masks the number cells (they differ every run); this proves the digits mean something -- a measured srtt, a bar level in range, and cmd_ms == look_ms + step_ms, the display's own definition of command latency.",
      "evidence": "invoked by tools/lint_repo.py (--selftest, the planted-log negatives) and as the net_hud scenario's post_check in tools/test_ui.py"
    },
    "check_net_lockstep_refs.py": {
      "class": "keep",
      "why": "mh_net split: the lockstep references the net module may hold",
      "evidence": "invoked by tools/lint_repo.py"
    },
    "check_net_session_addrs.py": {
      "class": "keep",
      "why": "mh_net split: the original-binary addresses the net module may name",
      "evidence": "invoked by tools/lint_repo.py"
    },
    "check_no_crash_marker.py": {
      "class": "keep",
      "why": "mp:CH1 (the AV clause): the gs2_quit_frozen UI scenario's cross-peer post_check -- the host quits a lockstep match frozen on a data-silent peer (the field a952c570_2 shape) and neither peer's process may write an mh_crash_*.marker",
      "evidence": "invoked by tools/test_ui.py's gs2_quit_frozen TESTS entry (post_check, post_check_peers) and tools/lint_repo.py (--selftest)"
    },
    "check_orders_agree.py": {
      "class": "keep",
      "why": "tooling:TL-SUITE-FOLD-DETC1 / dist:V022: match_launch_net's cross-peer post_check -- decodes each peer's mh_orders.bin (order_mode=1, via tools/mp_order_diff.py) and fails on a missing file, an empty recording, or a common-step disagreement; also run_det_config1's det_orders_verdict (det_dir/{peer} layout), folded here from the retired run_gate.py det_c1 unit",
      "evidence": "invoked by tools/uiscripts/registry.yaml's match_launch_net row (post_check, post_check_peers) and its own --selftest (planted mh_orders.bin files, including the process/session-dir indirection)"
    },
    "check_overlay_residue.py": {
      "class": "keep",
      "why": "mp:GX1: our DLL-native overlays (mh/seams/gfx_overlay.cpp, mh/seams/ui_net_indicator.cpp) must not smear on the ground layer's damage map -- reads a `capture <name>` pair off a UI-suite run and asserts pixels matching the overlay's OWN configured text colour are present where a wider/taller earlier frame drew them and absent once a narrower/shorter later frame no longer does. Colour-match, not a luminance threshold (a threshold false-positived on live strategic-ground terrain art, which crosses the same brightness bar as the overlay's own text).",
      "evidence": "invoked by tools/test_ui.py's gx1_overlay_residue TESTS entry (post_check) and tools/lint_repo.py (--selftest, 6 synthetic arms incl. the colour-blindness regression)"
    },
    "check_p6_gap.py": {
      "class": "keep",
      "why": "mp:P6: the p6_loser_gap UI scenario's post_check -- reads the host's and client's own `on_gameover ENTER` lines (winner/loser outcomes must differ), anchors each peer's own mh_frametime.log qpc_us to wall-clock the same way tools/mp_analyze.py's find_lobby_clip_qpc does (via that peer's own boot-session \"present hook armed\" line), and asserts (a) the host's own D21 in-band desync watch shows 0 mismatching over a non-vacuous compared-sample count (ALL PAIRS IDENTICAL) and (b) both the client's (loser's) and the host's (winner's) own longest present-to-present gap in the window [host's own outcome, client's own outcome] are under the bound -- the endgame-sync grace's (net_lockstep.cpp HB_ENDGAME_GRACE_MS) real, measured cost.",
      "evidence": "invoked by tools/test_ui.py's p6_loser_gap TESTS entry (post_check, post_check_peers, post_check_session) and tools/lint_repo.py (--selftest, 10 synthetic arms)"
    },
    "check_publishable.py": {
      "class": "keep",
      "why": "fork F5A: every tracked path carries exactly one publish disposition",
      "evidence": "invoked by tools/lint_repo.py"
    },
    "check_queue_rollups.py": {
      "class": "keep",
      "why": "mp:U41b -- two matches over one live link must write two per-match inbound-queue rollups, the second not inheriting the first's high-water or counters; reads each peer's process mh_net.log",
      "evidence": "invoked by tools/lint_repo.py (--selftest, the planted negative cases) and by tools/test_ui.py (rematch_rollup post_check_peers)"
    },
    "check_gone_peer_guard.py": {
      "class": "keep",
      "why": "mp:U19j: the `--u19j-gpfg3` 3-peer shape's verdict -- in CONFIGURATION (1) the gone-peer frame guard's byte-patch carrier (the splice over llm_net_lockstep_dispatch's kick re-broadcast @0x0049c330) FIRED on the leader after a frame arrived from a peer the survivors had already dropped, and both survivors kept playing; its --expect-red twin passes (XFAIL) only when the gone_peer_frame_guard=0 arm went red on exactly that garbled frame (outcome 7 raised inside lockstep)",
      "evidence": "invoked by tools/test_ui.py's run_u19j_gpfg3 (--u19j-gpfg3 / --u19j-gpfg3-unguarded) and tools/lint_repo.py (--selftest)"
    },
    "check_quit_survivors_3peer.py": {
      "class": "keep",
      "why": "mp:U19b: the `--u19b-quit3` determinism shape's post-check (the U19 3-player clean-quit clause no 2-peer scenario or AI seat can seat) -- the quitter's own U17 self-removal broadcast is on the wire, NEITHER survivor's match ends (no on_gameover at all) or learns of the departure via the B2 fast-drop catch, and mp_analyze.py over both survivors' logs prints the exact 'ALL PAIRS IDENTICAL (determinism-clean)' verdict over enough overlapping hashed steps to actually cover the post-drop portion -- reading mp_analyze's PRINTED verdict rather than its exit code, which is 0 even on a DESYNC",
      "evidence": "invoked by tools/test_ui.py's run_u19b_quit3 (--u19b-quit3) and tools/lint_repo.py (--selftest)"
    },
    "check_relay_leg.py": {
      "class": "keep",
      "why": "mp:R7a: the positive twin of check_direct_dial -- the relay_browse_local scenario's claim that a first-browser join really goes THROUGH the relay (a `udp relay leg UP` and a `udp path` line are present, and the relay carried the session), so a regression that silently made the first-browser path direct cannot pass.",
      "evidence": "invoked by tools/lint_repo.py (--selftest, the planted-log negatives) and as the relay_browse_local scenario's post_check in tools/test_ui.py"
    },
    "check_relay_path.py": {
      "class": "keep",
      "why": "mp:R3a: the relay_punch scenario's claim -- a mid-handshake hole-punch path switch does not break the link -- made an assertion rather than an implication, by reading `udp path DIRECT` off the host lane's mh_net.log.",
      "evidence": "invoked by tools/lint_repo.py (--selftest, the planted-log negatives) and as the relay_punch scenario's post_check in tools/test_ui.py"
    },
    "check_relay_restart.py": {
      "class": "keep",
      "why": "mp:R4b: the relay_restart scenario's claim -- a relayed pair survives the relay process restarting mid-match by re-HELLOing -- made an assertion rather than an implication, by reading `udp relay leg LOST` + `udp relay leg RESTORED` off both peers' mh_net.log and two `listening` lines off the relay's own log.",
      "evidence": "invoked by tools/lint_repo.py (--selftest, the planted-log negatives) and as the relay_restart scenario's post_check in tools/test_ui.py"
    },
    "check_relay_rooms.py": {
      "class": "keep",
      "why": "mp:R6: the relayed scenarios' claim that a HOST registers under a MINTED room (never its `[net] port`), that hosts in one run hold distinct rooms, that every client dialled a minted one through the directory, and that the relay refused no registration -- read off every peer's mh_net.log (`net: udp relay -- host room`, `leg UP ... room=`) and the relay's counters line, since a frame cannot show a room code.",
      "evidence": "invoked by tools/lint_repo.py (--selftest, the planted-log negatives) and as the relay_match / relay_browse scenarios' post_check in tools/test_ui.py"
    },
    "check_relay_stale.py": {
      "class": "keep",
      "why": "mp:R4a: the relay_stale_notice scenario's claim -- a peer that dialled a relay BEHIND its protocol level says so -- read off the logs the pixels cannot show: the one named `relay protocol <theirs> < <ours>` line in a peer's mh_net.log, the `; R4a: relay notice queued` line that proves the text reached mh.dll's browser carrier, and the relay's own protocol_level / hello_level_mismatch on its log. `--expect matching` is the negative every other relay scenario's pair must satisfy.",
      "evidence": "invoked by tools/lint_repo.py (--selftest, the planted-log negatives) and as the relay_stale_notice scenario's post_check in tools/test_ui.py"
    },
    "check_rematch_residue.py": {
      "class": "keep",
      "why": "mp:RM1: the rematch_play UI scenario's cross-peer post_check -- a SECOND match in one process enters session_begin_multi as clean as the first (gclk=0, p54bc=2, relation rows equal to game 1's and across peers), game 1's [desync] rollup compared >= 1 with 0 mismatching, game 2's first hashed sample identical on both peers",
      "evidence": "invoked by tools/test_ui.py's rematch_play TESTS entry (post_check, post_check_peers) and tools/lint_repo.py (--selftest)"
    },
    "check_resync_receiver_deadline.py": {
      "class": "keep",
      "why": "mp:P9W: proves a NON-LEADER left a leaderless mode-8 barrier through its OWN deadline (src/mh_dll/mh/seams/net_lockstep.cpp's resync_receiver_deadline_hook, [net] resync_receiver_deadline default ON) rather than through some other path (retail's normal-frame graceful_drop/data_timeout catching the dead transport BETWEEN barriers reaches the same below-quorum dialog, and a capture cannot tell the two apart) -- reads the `; [resync] receiver_deadline: ... ms in barrier with no RESUME -- leaving the wait screen and removing side_id=..` line (net.resync_receiver_deadline in log_formats.json).",
      "evidence": "invoked by tools/lint_repo.py (--selftest, the planted-log negatives, both --expect fired and --expect absent); NOT YET wired into a tools/test_ui.py post_check -- no share_lanes row proving the live rig repro was registered this session (2026-09-22 wave-3 lane C; see tracker/mp.yaml P9W progress for why), left for whoever next builds that scenario"
    },
    "check_resync_storm.py": {
      "class": "keep",
      "why": "mp:P9: the match_launch_net / resync_storm_repro UI scenarios' cross-peer post_check -- a configuration-(1) pair (libmh.dll omitted) must carry resync_trigger_gate through the re-instated byte patches (`; resync-trigger gate: 2/2 INC sites patched`, no UNCARRIED FIX line, zero `[resync] force_resync FIRED` over >= 1000 steps with the D21 desync watch agreeing); the ungated reproduction arm must show the storm (>= 3 FIRED lines)",
      "evidence": "invoked by tools/test_ui.py's match_launch_net and resync_storm_repro TESTS entries (post_check, post_check_peers, post_check_session) and tools/lint_repo.py (--selftest)"
    },
    "check_sim_rate.py": {
      "class": "keep",
      "why": "mp:P13: the sim rate of every peer, read off mh_lockstep.log (d clock / d wall after a 5 s warm-up, the worst 30 s window, mode-8 barrier rows) -- P13's done_when asks for the rate to be read by a checker, not eyeballed; the tested successor of the ad-hoc tmp/wave2_rig/rate.py",
      "evidence": "invoked by tools/lint_repo.py (--selftest, planted tables) and tools/test_ui.py's p13_rate_180 TESTS entry (post_check, post_check_peers, post_check_session); the selftest's (whole / window / mode-8 / too-short / missing-log negatives, each clause mutation-checked 2026-09-24)"
    },
    "check_roster_caps.py": {
      "class": "keep",
      "why": "no production index expression uses a compile-time roster cap",
      "evidence": "invoked by tools/lint_repo.py"
    },
    "check_save_block_slices.py": {
      "class": "keep",
      "why": "every save block is servable under a relocated bind",
      "evidence": "invoked by tools/lint_repo.py"
    },
    "check_selftest_roster.py": {
      "class": "keep",
      "why": "the committed selftest roster equals the exe's own suite table (fork F5I)",
      "evidence": "invoked by tools/lint_repo.py"
    },
    "check_ses6_delivery.py": {
      "class": "keep",
      "why": "mp:SES6: reads ONE peer's mh_net.log for the `net: udp counters` line's per-peer segments (net.udp_counters in log_formats.json) and asserts the outbound-delivery shape -- data_rx_age climbing, data_tx_age staying bounded (we kept sending), horizon_ms frozen (the peer's own sim stalled, not just a slow link) -- across a >=2-sample window. The gs2_data_timeout scenario's already-registered rig shape produces exactly this signature on the survivor's side.",
      "evidence": "invoked by tools/lint_repo.py (--selftest); NOT YET wired into a tools/test_ui.py post_check -- that wiring is a tooling-lane edit outside mp:SES6's write set (2026-09-21 wave-1 lane C), left for whoever next touches tools/test_ui.py's gs2_data_timeout entry"
    },
    "check_session_rollover.py": {
      "class": "keep",
      "why": "SES1b: the three-match session-rollover UI scenario's post_check -- 3 session directories per peer, pairwise-identical match_ids, each properly closed",
      "evidence": "invoked by tools/test_ui.py's session_rollover TESTS entry (post_check) and tools/lint_repo.py (--selftest)"
    },
    "check_shim_rtt.py": {
      "class": "keep",
      "why": "tooling:TL-SHIMUDP-C: a shimmed UDP match really measured a delayed round trip rather than a silently-bypassed shim, read off srtt0_ms in mh_lockstep.log (net_lockstep.cpp's RFC 6298 estimator). A registered scenario can look green with the shim skipped entirely -- the match still plays and captures clean -- and the median measured srtt is the one signal that tells a real ~2x-one-way-delay round trip apart from the LAN's real (near-zero) one.",
      "evidence": "invoked by tools/lint_repo.py (--selftest, the planted-log negatives) and as the shim_udp scenario's post_check in tools/test_ui.py"
    },
    "check_sim_addresses.py": {
      "class": "keep",
      "why": "no game-state address reaches a sim translation (W2)",
      "evidence": "invoked by tools/lint_repo.py"
    },
    "check_state_bindings.py": {
      "class": "keep",
      "why": "can a translation actually WRITE every region it claims",
      "evidence": "no machine invoker -- run by a translator before arming, to see whether a body can write what it claims",
      "hand_run": true
    },
    "check_tool_dispositions.py": {
      "class": "keep",
      "why": "fork F5G: this file's own gate -- every top-level tools/*.py carries a fork disposition and every keep row names what runs it",
      "evidence": "invoked by tools/lint_repo.py"
    },
    "check_transport_death.py": {
      "class": "keep",
      "why": "mp:U19f: the txdeath_ingame UI scenario's cross-peer post_check -- an IN-GAME (not lobby-phase, not a quit) net_shim blackhole must be seen through the fast-drop route (`; U17 fast-drop`), raise on_gameover outcome=7 (NETWORK_ERROR), never through mp:U19e's garbled-stream arm, and U19d's below-quorum wording correction must NOT fire on a real transport death -- the negative arm that discriminator never had",
      "evidence": "invoked by tools/test_ui.py's txdeath_ingame TESTS entry (post_check, post_check_peers) and tools/lint_repo.py (--selftest)"
    },
    "check_u39_echo.py": {
      "class": "keep",
      "why": "mp:U39: the u39_diplomacy_echo UI scenario's cross-peer post_check (the NEGATIVE arm) -- with the retail relation echo kept ([net] diplo_echo_nop=0) the pair's state hash must differ for a bounded window (1..16 steps, 4 measured) with `players` among the diverging regions at the first mismatch, and re-converge by the last common step; proves u39_diplomacy is green for the right reason",
      "evidence": "invoked by tools/test_ui.py's u39_diplomacy_echo TESTS entry (post_check, post_check_peers) and tools/lint_repo.py (--selftest)"
    },
    "classify_reg_preservation.py": {
      "class": "archive",
      "why": "Ghidra per-function register-preservation classifier",
      "evidence": "F0 triage (docs/fork-inventory.md section 1): the Ghidra DB pipeline (the fork freezes the database)"
    },
    "commit_prototypes.py": {
      "class": "archive",
      "why": "naming-pipeline prototype committer (Ghidra write)",
      "evidence": "F0 triage (docs/fork-inventory.md section 1): the naming/documentation pipeline (Ghidra-side, dropped with the DB)"
    },
    "coverage.py": {
      "class": "keep",
      "why": "execution-lane coverage of our C++ bodies (OpenCppCoverage)",
      "evidence": "invoked by tools/lint_repo.py"
    },
    "crash_report.py": {
      "class": "keep",
      "why": "finds/attributes Windows crash reports for a rig peer",
      "evidence": "invoked by tools/lint_repo.py"
    },
    "deploy_relay_direct.py": {
      "class": "keep",
      "why": "hand-run: the relay's iteration deploy path -- local musl cross-build -> scp -> image built on the VPS -> compose up, bypassing images.yml/deploy.yml (the release path). docs/deploy.md section 4a.",
      "evidence": "added 2026-09-19 (user: CI too long per rebuild); first run put the R4b relay on the VPS as dev-d148460b0f, restart 0.94 s. Reads machine_config.VPS_HOST/VPS_SSH_KEY, refuses when empty.",
      "hand_run": true
    },
    "desktop.py": {
      "class": "keep",
      "why": "launches a process on its own Windows desktop (headless rig)",
      "evidence": "used by mp_run.py, test_ui.py, ui_test.py"
    },
    "det_arms.py": {
      "class": "keep",
      "why": "tooling:TL-SUITE-SPLIT: the determinism arms split out of test_ui.py -- the 2-peer UI-path lockstep gate and its shapes (--det-standard, --det-config1, --det-3peer, --u19b-quit3), the single-player oracle (--sp-determinism) and the 3-peer / VM-only proofs (--l1f-ping3, --u19j-gpfg3, --x2a-map-variant)",
      "evidence": "invoked by tools/run_gate.py (det, det_c1, u19j_gpfg3, x2a_map_variant, spdet units) and tools/lint_repo.py (--det-selftest); test_ui.py forwards its old --determinism/--sp-determinism/... flags here"
    },
    "drain_reports.py": {
      "class": "keep",
      "why": "dist RP3: pulls new report directories off the VPS collector (dist:RP2) over a read-only rrsync key, into a local gitignored store, and prints one summary line per new report",
      "evidence": "invoked by tools/lint_repo.py (--selftest)"
    },
    "dump_addr_index.py": {
      "class": "archive",
      "why": "Ghidra address->name index dump (for docs)",
      "evidence": "F0 triage (docs/fork-inventory.md section 1): the Ghidra DB pipeline (the fork freezes the database)"
    },
    "dump_addr_taken.py": {
      "class": "archive",
      "why": "Ghidra address-taken (indirect entry) dump",
      "evidence": "F0 triage (docs/fork-inventory.md section 1): the Ghidra DB pipeline (the fork freezes the database)"
    },
    "dump_ai_gate_sites.py": {
      "class": "archive",
      "why": "Ghidra AI/sim gate-site dump",
      "evidence": "F0 triage (docs/fork-inventory.md section 1): the Ghidra DB pipeline (the fork freezes the database)"
    },
    "dump_aux_symbols.py": {
      "class": "archive",
      "why": "Ghidra non-primary-program symbol dump",
      "evidence": "F0 triage (docs/fork-inventory.md section 1): the Ghidra DB pipeline (the fork freezes the database)"
    },
    "dump_call_graph.py": {
      "class": "archive",
      "why": "Ghidra call-graph dump",
      "evidence": "F0 triage (docs/fork-inventory.md section 1): the Ghidra DB pipeline (the fork freezes the database)"
    },
    "dump_call_protos.py": {
      "class": "archive",
      "why": "Ghidra calling-contract dump (feeds gen_dll_calls.py)",
      "evidence": "F0 triage (docs/fork-inventory.md section 1): the Ghidra DB pipeline (the fork freezes the database)"
    },
    "dump_epilogue_returns.py": {
      "class": "archive",
      "why": "Ghidra epilogue-return-value dump (feeds lint_dropped_returns.py)",
      "evidence": "F0 triage (docs/fork-inventory.md section 1): the Ghidra DB pipeline (the fork freezes the database)"
    },
    "dump_functions.py": {
      "class": "archive",
      "why": "Ghidra function-extent dump (feeds gen_dll_patches.py)",
      "evidence": "F0 triage (docs/fork-inventory.md section 1): the Ghidra DB pipeline (the fork freezes the database)"
    },
    "dump_interior_refs.py": {
      "class": "archive",
      "why": "Ghidra body-shape hazard dump (feeds gen_tombstones.py)",
      "evidence": "F0 triage (docs/fork-inventory.md section 1): the Ghidra DB pipeline (the fork freezes the database)"
    },
    "dump_order_matrix.py": {
      "class": "archive",
      "why": "Ghidra order-routing dump",
      "evidence": "F0 triage (docs/fork-inventory.md section 1): the Ghidra DB pipeline (the fork freezes the database)"
    },
    "dump_steal_relbranch.py": {
      "class": "archive",
      "why": "Ghidra trampoline-steal-byte safety dump",
      "evidence": "F0 triage (docs/fork-inventory.md section 1): the Ghidra DB pipeline (the fork freezes the database)"
    },
    "dump_struct_layouts.py": {
      "class": "archive",
      "why": "Ghidra struct-layout dump (feeds gen_dll_structs.py)",
      "evidence": "F0 triage (docs/fork-inventory.md section 1): the Ghidra DB pipeline (the fork freezes the database)"
    },
    "dump_structs.py": {
      "class": "archive",
      "why": "regenerates docs/structs.md from the live Ghidra DB",
      "evidence": "F0 triage (docs/fork-inventory.md section 1): the Ghidra DB pipeline (the fork freezes the database)"
    },
    "dump_symbols.py": {
      "class": "archive",
      "why": "regenerates docs/symbols.md + symbol index from the live DB",
      "evidence": "F0 triage (docs/fork-inventory.md section 1): the Ghidra DB pipeline (the fork freezes the database)"
    },
    "ensure_reva.py": {
      "class": "archive",
      "why": "auto-launches headless ReVA/Ghidra",
      "evidence": "F0 triage (docs/fork-inventory.md section 1): the Ghidra DB pipeline (the fork freezes the database)"
    },
    "export_decomp.py": {
      "class": "archive",
      "why": "bulk decompile export (Ghidra)",
      "evidence": "F0 triage (docs/fork-inventory.md section 1): the Ghidra DB pipeline (the fork freezes the database)"
    },
    "export_metadata.py": {
      "class": "archive",
      "why": "Ghidra DB metadata (our work-product) exporter",
      "evidence": "F0 triage (docs/fork-inventory.md section 1): the Ghidra DB pipeline (the fork freezes the database)"
    },
    "finalize_data_naming.py": {
      "class": "archive",
      "why": "naming-pipeline apply-prep",
      "evidence": "F0 triage (docs/fork-inventory.md section 1): the naming/documentation pipeline (Ghidra-side, dropped with the DB)"
    },
    "fixture_replay.py": {
      "class": "keep",
      "why": "LIB-REF replay fixture: pack/verify/replay (the recorder tool)",
      "evidence": "used by replay_libref.py"
    },
    "gate_timeline.py": {
      "class": "keep",
      "why": "per-lane gate-run timeline/utilization report",
      "evidence": "no machine invoker -- run by a human reading a finished gate run's lane utilization",
      "hand_run": true
    },
    "gen_boot_snapshot.py": {
      "class": "keep",
      "why": "LIB-BOOT cfg-snapshot schema (ownership-style verdicts, but feeds a real boot artifact). KEEP: lint_repo drift gate and its --check runs off committed inputs (no Ghidra), and LIB-BOOT is libmh machinery the fork keeps",
      "evidence": "invoked by tools/lint_repo.py"
    },
    "gen_codemap.py": {
      "class": "archive",
      "why": "Ghidra coverage HTML report",
      "evidence": "F0 triage (docs/fork-inventory.md section 1): the Ghidra DB pipeline (the fork freezes the database)"
    },
    "gen_dll_addrs.py": {
      "class": "keep",
      "why": "generates mh_addrs.gen.h (Ghidra-free, reads committed JSON)",
      "evidence": "invoked by tools/lint_repo.py"
    },
    "gen_dll_calls.py": {
      "class": "keep",
      "why": "generates mh_calls.gen.{h,cpp} (Ghidra-free)",
      "evidence": "invoked by tools/lint_repo.py"
    },
    "gen_dll_exports.py": {
      "class": "keep",
      "why": "generates mh_export.gen.h (Ghidra-free)",
      "evidence": "invoked by tools/lint_repo.py"
    },
    "gen_dll_patches.py": {
      "class": "keep",
      "why": "generates mh_patches.gen.h, extents of promoted functions",
      "evidence": "invoked by tools/lint_repo.py"
    },
    "gen_dll_structs.py": {
      "class": "keep",
      "why": "generates mh_structs.gen.h (Ghidra-free)",
      "evidence": "invoked by tools/lint_repo.py"
    },
    "gen_export_request.py": {
      "class": "archive",
      "why": "migration-loop AI-cluster export-request builder",
      "evidence": "F0 triage (docs/fork-inventory.md section 1): the migration / promotion / ledger machinery the fork drops"
    },
    "gen_fallthrough_entries.py": {
      "class": "archive",
      "why": "Ghidra fall-through-edge dump (feeds check_arming_set.py)",
      "evidence": "F0 triage (docs/fork-inventory.md section 1): the Ghidra DB pipeline (the fork freezes the database)"
    },
    "gen_harness_contract.py": {
      "class": "keep",
      "why": "the harness contract rows (the 13-row instrument/hook table) generated from the sources rather than typed",
      "evidence": "invoked by tools/lint_repo.py, tools/run_gate.py"
    },
    "gen_hostapi_callers.py": {
      "class": "keep",
      "why": "--check guards tools/data/hostapi_callers.json against libmh/include/libmh_host_api.gen.h + libmh_tact_host_api.gen.h, two headers the public build compiles -- two 2026-09-10 slices shrank those tables without regenerating the evidence",
      "evidence": "fork F5M S4a (2026-09-16) re-triage, one rule: is this a live drift gate over a file the PUBLIC build compiles or the public tree ships? The F0 class answered a different question (does the fork still need the Ghidra pipeline) and archived gates the public tree wants."
    },
    "gen_inmem_manifest.py": {
      "class": "keep",
      "why": "the in-memory patch manifest, derived from the static patch set",
      "evidence": "invoked by tools/lint_repo.py"
    },
    "gen_libmh_calls.py": {
      "class": "keep",
      "why": "--check guards tools/data/libmh_call_census.json, the outward-call census over src/mh_dll/mh/** that the public build compiles; a new mh::call:: site without a regenerated census is exactly the drift the lib-complete number exists to see",
      "evidence": "fork F5M S4a (2026-09-16) re-triage, one rule: is this a live drift gate over a file the PUBLIC build compiles or the public tree ships? The F0 class answered a different question (does the fork still need the Ghidra pipeline) and archived gates the public tree wants."
    },
    "gen_libmh_contract.py": {
      "class": "keep",
      "why": "libmh's build contract: the source roster generated against the vcxproj",
      "evidence": "invoked by src/mh_dll/libmh_dll/libmh_dll.vcxproj, tools/lint_repo.py, tools/run_gate.py"
    },
    "gen_libmh_hostapi.py": {
      "class": "keep",
      "why": "generates host-callback ABI tables (real build artifact; sourced from census/ledger data). KEEP: emits a real libmh build artifact and lint gates it",
      "evidence": "invoked by tools/lint_repo.py"
    },
    "gen_libmh_inbound.py": {
      "class": "keep",
      "why": "LIB-REF-IN inbound-surface derivation (routing table vs. census). KEEP: lint-gated derivation of libmh's inbound surface",
      "evidence": "invoked by tools/lint_repo.py"
    },
    "gen_libmh_rebind.py": {
      "class": "keep",
      "why": "rebind-row generator (explicitly dropped). F0 SAID ARCHIVE AND THAT WAS WRONG (corrected F5G, re-measured at HEAD): lint_repo.py gates `gen_libmh_rebind --check` on every run, and its data is live on both sides -- tools/data/libmh_rebind.json + libmh_rebind_targets.json feed the binder, and test_ui.py reads rebind_yield_dispositions.json to decide whether a YIELDED row costs a promotion (SIM1-P clause 6b). What F2 dropped was the rebind CONFIGURATION surface, not the binder generator",
      "evidence": "invoked by tools/lint_repo.py"
    },
    "gen_libmh_std_contract.py": {
      "class": "keep",
      "why": "libmh_std's build contract, same generator family",
      "evidence": "invoked by tools/lint_repo.py, tools/run_gate.py"
    },
    "gen_libmh_vcxproj.py": {
      "class": "keep",
      "why": "generates libmh.vcxproj, the static-lib build project",
      "evidence": "invoked by src/mh_dll/libmh/libmh.vcxproj, src/mh_dll/libmh_dll/libmh_dll.vcxproj, src/mh_dll/libmh_std/libmh_std.vcxproj, tools/lint_repo.py"
    },
    "gen_libref_stock_bases.py": {
      "class": "keep",
      "why": "importing-host stock-base table for LIB-REF-SPLIT",
      "evidence": "no machine invoker -- run by a one-per-change regeneration of the LIB-REF-SPLIT stock-base table (its output is committed, so nothing has to run it per gate)",
      "hand_run": true
    },
    "gen_lzw_fixtures.py": {
      "class": "keep",
      "why": "recorder: builds savetest LZW fixture from a real save",
      "evidence": "no machine invoker -- run by a bring-your-own-game user regenerating the savetest fixture locally -- the recorder ships, the recording does not (fork-inventory section 3)",
      "hand_run": true
    },
    "gen_migration.py": {
      "class": "keep",
      "why": "carried as a LIBRARY, not as a generator: gen_libmh_inbound.py (keep, a lint_repo row) imports it for load_addr_taken()/orphan_closure(). Its manifest-REGENERATION path imports migration_seeds.py and migration_ledger.py, which stay archive -- both imports are lazy and that path refuses with a named reason off the private tree",
      "evidence": "fork F5M S4a (2026-09-16): re-classed from archive when the archive-class withhold rule was drawn -- a keep gate IMPORTS this module, so the public tree cannot run its own lint without it. Measured by import closure over the keep set; the publish-ledger closure arm cannot see this class of edge, because it scans path-form strings and `import _cstrip` has none."
    },
    "gen_module_calls.py": {
      "class": "keep",
      "why": "emits a reimpl module's calls-table snippet from generated headers",
      "evidence": "no machine invoker -- run by a translator opening a new reimpl module",
      "hand_run": true
    },
    "gen_no_cd_manifest.py": {
      "class": "keep",
      "why": "bakes the no-CD mhpatch manifest",
      "evidence": "used by mp_run.py"
    },
    "gen_notify_readback.py": {
      "class": "keep",
      "why": "notify-scope write/readback census, doubling as an ABI-hazard gate. KEEP: lint-gated ABI-hazard gate over the notify scopes",
      "evidence": "invoked by tools/lint_repo.py"
    },
    "gen_order_issue_golden.py": {
      "class": "keep",
      "why": "derives the order-issue oracle mechanically from disassembly. KEEP: lint-gated, and --check verifies the COMMITTED outputs, so it runs with the DB frozen (whether the derived header ships is publish_ledger's question)",
      "evidence": "invoked by tools/lint_repo.py"
    },
    "gen_order_issue_inventory.py": {
      "class": "archive",
      "why": "order-issue ownership inventory (census)",
      "evidence": "F0 triage (docs/fork-inventory.md section 1): the migration / promotion / ledger machinery the fork drops"
    },
    "gen_order_matrix.py": {
      "class": "archive",
      "why": "order issue-dispatch coverage matrix",
      "evidence": "F0 triage (docs/fork-inventory.md section 1): the migration / promotion / ledger machinery the fork drops"
    },
    "gen_pool_reloc_manifest.py": {
      "class": "archive",
      "why": "Ghidra-dependent array-relocation patch-manifest generator",
      "evidence": "F0 triage (docs/fork-inventory.md section 1): the Ghidra DB pipeline (the fork freezes the database)"
    },
    "gen_proxy_stubs.py": {
      "class": "keep",
      "why": "generates the DLL-injection proxy-shim forwarding stubs",
      "evidence": "invoked by src/mh_shim/msvfw32/msvfw32_shim.vcxproj"
    },
    "gen_reg_clobberers.py": {
      "class": "archive",
      "why": "turns a Ghidra classifier's output into cspec-model manifests",
      "evidence": "F0 triage (docs/fork-inventory.md section 1): the Ghidra DB pipeline (the fork freezes the database)"
    },
    "gen_region_accessors.py": {
      "class": "keep",
      "why": "--check guards tools/data/region_accessors.json against the region registry, and gen_state_registry.py (keep) IMPORTS its currency() for rules 4-6 before emitting mh/addr/mh_regions.gen.h, which the public build compiles",
      "evidence": "fork F5M S4a (2026-09-16) re-triage, one rule: is this a live drift gate over a file the PUBLIC build compiles or the public tree ships? The F0 class answered a different question (does the fork still need the Ghidra pipeline) and archived gates the public tree wants."
    },
    "gen_save_block_table.py": {
      "class": "archive",
      "why": "derives the save block table from Ghidra-exported .asm. ARCHIVE: a one-shot derivation from Ghidra .asm exports (--asm-dir), with no --check mode and no invoker -- the table it produced is committed",
      "evidence": "F0 triage (docs/fork-inventory.md section 1): the Ghidra DB pipeline (the fork freezes the database)"
    },
    "gen_save_table_header.py": {
      "class": "keep",
      "why": "generates save_table.gen.h from committed JSON",
      "evidence": "invoked by tools/lint_repo.py"
    },
    "gen_seam_surface.py": {
      "class": "archive",
      "why": "migration-item call-out risk/cost measurer",
      "evidence": "F0 triage (docs/fork-inventory.md section 1): the migration / promotion / ledger machinery the fork drops"
    },
    "gen_session_report.py": {
      "class": "keep",
      "why": "MP run HTML/SVG perf report (hardcoded paths need refresh)",
      "evidence": "no machine invoker -- run by a human reading an MP run",
      "hand_run": true
    },
    "gen_st0_goldens.py": {
      "class": "keep",
      "why": "recorder: lifts the ST0 sin/cos oracle from a rig log",
      "evidence": "no machine invoker -- run by a bring-your-own-game user regenerating the ST0 oracle from a rig log -- same recorder-ships-not-the-recording rule",
      "hand_run": true
    },
    "gen_state_handler_table.py": {
      "class": "keep",
      "why": "extracts strategic dispatch tables from disassembly. KEEP: lint-gated drift check over committed headers (--extract is the Ghidra-only half and is not what the gate runs)",
      "evidence": "invoked by tools/lint_repo.py"
    },
    "gen_state_matrix.py": {
      "class": "archive",
      "why": "Ghidra (region x function) read/write matrix",
      "evidence": "F0 triage (docs/fork-inventory.md section 1): the Ghidra DB pipeline (the fork freezes the database)"
    },
    "gen_state_registry.py": {
      "class": "keep",
      "why": "state-region registry merge + ownership rules. KEEP: lint-gated, and its docstring states the inputs are committed so --check stays Ghidra-free",
      "evidence": "invoked by tools/lint_repo.py"
    },
    "gen_tact_closure.py": {
      "class": "archive",
      "why": "tactical membership closure (Ghidra call-graph walk)",
      "evidence": "F0 triage (docs/fork-inventory.md section 1): the migration / promotion / ledger machinery the fork drops"
    },
    "gen_tombstones.py": {
      "class": "keep",
      "why": "--check guards src/mh_dll/mh/addr/mh_tombstones.gen.h ITSELF -- a published header compiled into mh.dll through mh/seams/net_seams.cpp; a ledger row marked dead without a regenerated table ships an unarmed claim",
      "evidence": "fork F5M S4a (2026-09-16) re-triage, one rule: is this a live drift gate over a file the PUBLIC build compiles or the public tree ships? The F0 class answered a different question (does the fork still need the Ghidra pipeline) and archived gates the public tree wants."
    },
    "gen_update_manifest.py": {
      "class": "keep",
      "why": "dist LA2: builds and minisign-signs the manifest.json the launcher fetches -- the publishing half of the update path. Reads dist/'s three zips plus SHA256SUMS, re-hashes every asset and refuses a digest the packager did not compute, an incomplete configuration set, or a password-protected signing key. --genkey makes the key pair whose PUBLIC half is compiled into src/launcher/src/update.rs; the secret half never enters the tree",
      "evidence": "invoked by tools/lint_repo.py (--selftest), and by .github/workflows/release.yml once dist LA3 lands; src/launcher/tests/data/manifest.json is its output, verified in-tree by the minisign-verify crate"
    },
    "gen_va_census.py": {
      "class": "keep",
      "why": "the LIB-VA ratchet: --check guards tools/data/va_census.json, the committed per-site baseline over the `mh::call::` tokens in src/mh_dll/mh and libmh -- sources the public build compiles -- and it fails in BOTH directions, so a VA call entering or leaving the public tree is caught",
      "evidence": "fork F5M S4a (2026-09-16) re-triage, one rule: is this a live drift gate over a file the PUBLIC build compiles or the public tree ships? The F0 class answered a different question (does the fork still need the Ghidra pipeline) and archived gates the public tree wants."
    },
    "gen_vcxproj_filters.py": {
      "class": "keep",
      "why": "fork F5P: derives every hand project's .vcxproj.filters from its .vcxproj by one mechanical rule (folder = the item's on-disk directory), and owns the rule the three GENERATED libmh projects' filters are rendered with too. KEEP: lint-gated both ways (--check + --selftest), and the .filters files it emits are committed build inputs",
      "evidence": "invoked by tools/lint_repo.py, tools/gen_libmh_vcxproj.py"
    },
    "gen_world_snapshot.py": {
      "class": "keep",
      "why": "LIB-WORLD step-0 world schema (feeds fixture_replay.py). KEEP: lint-gated accounting for LIB-WORLD, which fixture_replay needs",
      "evidence": "invoked by tools/lint_repo.py"
    },
    "gen_writer_attribution.py": {
      "class": "archive",
      "why": "writer-ownership CENSUS (explicit)",
      "evidence": "F0 triage (docs/fork-inventory.md section 1): the migration / promotion / ledger machinery the fork drops"
    },
    "ghidra_write.py": {
      "class": "archive",
      "why": "manual Ghidra write-lease helper",
      "evidence": "F0 triage (docs/fork-inventory.md section 1): the Ghidra DB pipeline (the fork freezes the database)"
    },
    "hostlock.py": {
      "class": "keep",
      "why": "host-global leases (rig lease stays needed; Ghidra-write lease moot)",
      "evidence": "invoked by tools/lint_repo.py"
    },
    "install_ghidra_shims.py": {
      "class": "archive",
      "why": "Ghidra script-shim installer",
      "evidence": "F0 triage (docs/fork-inventory.md section 1): the Ghidra DB pipeline (the fork freezes the database)"
    },
    "journal_coverage.py": {
      "class": "keep",
      "why": "recorded-session order-op/UI coverage measurer",
      "evidence": "no machine invoker -- run by a human deciding whether a recorded session covers what it claims",
      "hand_run": true
    },
    "lane_alloc.py": {
      "class": "keep",
      "why": "fork F4H: the ONE place a lane number comes from, plus the disjointness gate",
      "evidence": "invoked by tools/lint_repo.py"
    },
    "lint_annotations.py": {
      "class": "archive",
      "why": "Ghidra DB annotation consistency checks",
      "evidence": "F0 triage (docs/fork-inventory.md section 1): the Ghidra DB pipeline (the fork freezes the database)"
    },
    "lint_arch.py": {
      "class": "archive",
      "why": "target-vs-measured architecture diff (Ghidra-derived matrix)",
      "evidence": "F0 triage (docs/fork-inventory.md section 1): the Ghidra DB pipeline (the fork freezes the database)"
    },
    "lint_citations.py": {
      "class": "keep",
      "why": "fork F5E: no PUBLISHED file may cite a path the public tree does not carry, and no file may cite a path that does not exist -- the prose/comment counterpart to check_publishable --closure's build-edge scan, which cannot see comments or .md by construction",
      "evidence": "fork F5E step 11 (2026-09-16): WIRED. lint_repo.py runs it as two rows -- 'fork F5E citations: no published file cites a path the cut does not carry (--strict)' and 'fork F5E citations -- the negative cases still fire' (--selftest) -- so the hand_run marker it carried between step 1 and step 11 is gone and check (d) is satisfied by a real invoker. The --strict row is in lint_repo's CI_SKIPS (measured: on the public tree the remaining private citation targets are absent, so the DANGLING arm fires on all of them and both allowlist rules go dead -- it gates the CUT, not the cut's output); the --selftest row runs in CI unskipped."
    },
    "lint_compose.py": {
      "class": "keep",
      "why": "dist RP4: the compose-file gate -- the relay service must run with network_mode: host, the collector must not, and nothing may reference Watchtower",
      "evidence": "invoked by tools/lint_repo.py"
    },
    "lint_detail_calls.py": {
      "class": "keep",
      "why": "no live_*_calls() inside a detail:: (offline-mockable) body",
      "evidence": "invoked by tools/lint_repo.py"
    },
    "lint_dll_patches.py": {
      "class": "keep",
      "why": "static half of the patch/seam interlock",
      "evidence": "invoked by tools/lint_repo.py"
    },
    "lint_doc_symbols.py": {
      "class": "archive",
      "why": "docs-vs-generated-symbol-table lint",
      "evidence": "F0 triage (docs/fork-inventory.md section 1): the research-doc + diary lints the fork does not ship"
    },
    "lint_dropped_returns.py": {
      "class": "keep",
      "why": "a promoted body may not drop a return its caller reads",
      "evidence": "invoked by tools/lint_repo.py"
    },
    "lint_dump_coherence.py": {
      "class": "keep",
      "why": "guards tools/data/dll_call_protos.json -- published since the Q3 flip, and the single input all five generated-header drift gates (addr/struct/calls/exports/shadow) compare against, so a stale dump is not drift to any of them",
      "evidence": "fork F5M S4a (2026-09-16) re-triage, one rule: is this a live drift gate over a file the PUBLIC build compiles or the public tree ships? The F0 class answered a different question (does the fork still need the Ghidra pipeline) and archived gates the public tree wants."
    },
    "lint_encoding.py": {
      "class": "keep",
      "why": "mojibake encoding lint (general mechanism, research-doc motivated). KEEP: a lint_repo row and a generic mechanism -- mojibake is not a research-doc-only failure",
      "evidence": "invoked by tools/lint_repo.py"
    },
    "lint_evidence_tiers.py": {
      "class": "archive",
      "why": "migration-ledger evidence-tier consistency",
      "evidence": "F0 triage (docs/fork-inventory.md section 1): the migration / promotion / ledger machinery the fork drops"
    },
    "lint_fixture_currency.py": {
      "class": "keep",
      "why": "tooling TL-GATE-D25FX: every recorded `state` artifact (libref fixtures, UI-REC oracles) is stamped with the CURRENT hash-manifest fingerprint and every A/B/C excusal names a region the manifest still has -- the gate for 'a manifest change means a re-capture in the same session'",
      "evidence": "invoked by tools/lint_repo.py"
    },
    "lint_fp_flags.py": {
      "class": "keep",
      "why": "x87 compile-flag coverage for sim/AI translation units",
      "evidence": "invoked by tools/lint_repo.py"
    },
    "lint_fp_helpers.py": {
      "class": "keep",
      "why": "mh/fp/ helper state-declaration check",
      "evidence": "invoked by tools/lint_repo.py"
    },
    "lint_hash_epoch.py": {
      "class": "keep",
      "why": "tooling TL-GATE8: the code that decides which bytes reach the state hash (tools/data/hash_input_epoch.json's watched files and HASH-INPUT spans) may not change without a HASH_INPUT_EPOCH decision (bump, or --neutral)",
      "evidence": "invoked by tools/lint_repo.py"
    },
    "lint_ini_string_keys.py": {
      "class": "keep",
      "why": "2026-09-20 (user ruling): no string-valued key in mh_net.example.ini may carry a same-line comment -- GetPrivateProfileStringA returns it as the value (probe_text drew its comment on every present of a stock install; relay= dialled its comment). The key set is derived from the DLL's readers, so the row cannot fall behind a new string key.",
      "evidence": "invoked by tools/lint_repo.py"
    },
    "lint_internal_edges.py": {
      "class": "keep",
      "why": "no our-code-to-our-code call via an original entry (lockstep/net)",
      "evidence": "invoked by tools/lint_repo.py"
    },
    "lint_libmh_layering.py": {
      "class": "keep",
      "why": "no libmh module reaches the injection harness unguarded",
      "evidence": "invoked by tools/lint_repo.py"
    },
    "lint_log_formats.py": {
      "class": "keep",
      "why": "mp:SES2: the log line format registry gate -- a parser's needle, the registry entry and the C++ emitter must still agree",
      "evidence": "invoked by tools/lint_repo.py"
    },
    "lint_machine_paths.py": {
      "class": "keep",
      "why": "guards against reintroduced machine-specific paths",
      "evidence": "invoked by tools/lint_repo.py"
    },
    "lint_md_tables.py": {
      "class": "keep",
      "why": "generic markdown-table-rendering hygiene (research-doc motivated). KEEP: a lint_repo row and generic markdown hygiene",
      "evidence": "invoked by tools/lint_repo.py"
    },
    "lint_migration_items.py": {
      "class": "archive",
      "why": "migration-domain state-interface-item check",
      "evidence": "F0 triage (docs/fork-inventory.md section 1): the migration / promotion / ledger machinery the fork drops"
    },
    "lint_notes_index.py": {
      "class": "archive",
      "why": "NOTES.md session-log index format (research diary)",
      "evidence": "F0 triage (docs/fork-inventory.md section 1): the research-doc + diary lints the fork does not ship"
    },
    "lint_proto_shapes.py": {
      "class": "keep",
      "why": "mh_calls.gen.h vs mh_export.gen.h shape consistency",
      "evidence": "invoked by tools/lint_repo.py"
    },
    "lint_region_mirror.py": {
      "class": "keep",
      "why": "determinism-harness region-manifest mirror check",
      "evidence": "invoked by tools/lint_repo.py"
    },
    "lint_repo.py": {
      "class": "keep",
      "why": "the master Ghidra-free repo gate (unit list needs heavy pruning)",
      "evidence": "invoked by tools/run_gate.py"
    },
    "lint_requirements.py": {
      "class": "keep",
      "why": "tools/requirements.txt completeness guard",
      "evidence": "invoked by tools/lint_repo.py"
    },
    "lint_resources.py": {
      "class": "keep",
      "why": "tooling:TL-SUITE-RESREG: no literal machine-global port/%TEMP% dir/lock/mutex in tools/*.py or *.bat that tools/data/resource_registry.json does not name -- the recurring parallel-agent collision shape (TL-RIG6/7/8, TL-LOCKRACE, TL-LANECOLLIDE, ...) fixed one incident at a time",
      "evidence": "invoked by tools/lint_repo.py"
    },
    "lint_rust.py": {
      "class": "keep",
      "why": "dist DS1: the Rust half of the repo gate -- cargo fmt --all --check and cargo clippy --workspace --all-targets -- -D warnings over the src/launcher + src/relay workspace, with a three-valued verdict so a machine without cargo SKIPS the rows by name instead of passing them vacuously",
      "evidence": "invoked by tools/lint_repo.py (two rust_row() rows plus its own --selftest row)"
    },
    "lint_source_narration.py": {
      "class": "keep",
      "why": "bans session-diary narration comments in src/mh_dll",
      "evidence": "invoked by tools/lint_repo.py"
    },
    "lint_strided_regions.py": {
      "class": "keep",
      "why": "a row-strided region must be longer than one row",
      "evidence": "invoked by tools/lint_repo.py"
    },
    "lint_translation.py": {
      "class": "archive",
      "why": "migration-translation region/callee-set reviewer",
      "evidence": "F0 triage (docs/fork-inventory.md section 1): the migration / promotion / ledger machinery the fork drops"
    },
    "lint_translation_sets.py": {
      "class": "archive",
      "why": "same, sim-domain (no state-header parsing) variant",
      "evidence": "F0 triage (docs/fork-inventory.md section 1): the migration / promotion / ledger machinery the fork drops"
    },
    "lint_workflows.py": {
      "class": "archive",
      "why": "validates .claude/workflows/*.js (naming/migration workflows)",
      "evidence": "F0 triage (docs/fork-inventory.md section 1): the research-doc + diary lints the fork does not ship"
    },
    "lint_x87_asm.py": {
      "class": "keep",
      "why": "inline x87 asm may only shrink, and only inside mh/fp/",
      "evidence": "invoked by tools/lint_repo.py"
    },
    "loop_worktree.py": {
      "class": "archive",
      "why": "migration-loop git-worktree runner",
      "evidence": "F0 triage (docs/fork-inventory.md section 1): the migration / promotion / ledger machinery the fork drops"
    },
    "machine_config.py": {
      "class": "keep",
      "why": "the one machine-specific-constant module",
      "evidence": "used by bootstrap.py, check_const_view.py, check_inmem_patch_parity.py, check_module_bind.py, check_net_lockstep_refs.py, check_net_session_addrs.py, coverage.py, gen_harness_contract.py, gen_libmh_contract.py, gen_libref_stock_bases.py, gen_no_cd_manifest.py, gen_session_report.py, hostlock.py, lint_machine_paths.py, lint_repo.py, make_lane.py, mp_prep_manual.py, mp_run.py, mp_run_sweep.py, mp_step_sweep.py, provision_rig.py, replay_libref.py, rig_batch.py, run_gate.py, test_ui.py, ui_test.py"
    },
    "make_lane.py": {
      "class": "keep",
      "why": "builds a parallel rig test-lane install",
      "evidence": "used by check_inmem_patch_parity.py, gate_timeline.py, mp_run.py, test_ui.py, ui_test.py"
    },
    "map_variant.py": {
      "class": "keep",
      "why": "mp:T5/X2a -- makes a map file's content differ from a base copy (same name, same length) so a rig lane can never already hold the map by the game's content-hash check; also a standalone --check offline arm",
      "evidence": "imported by make_lane.py's --map-variant option"
    },
    "materialize_fnptr_refs.py": {
      "class": "archive",
      "why": "Ghidra function-pointer reference materializer",
      "evidence": "F0 triage (docs/fork-inventory.md section 1): the Ghidra DB pipeline (the fork freezes the database)"
    },
    "migration_ab.py": {
      "class": "archive",
      "why": "promoted-golden A/B driver for dispatcher reimplementations",
      "evidence": "F0 triage (docs/fork-inventory.md section 1): the migration / promotion / ledger machinery the fork drops"
    },
    "migration_domain.py": {
      "class": "keep",
      "why": "the domain-profile loader; imported at module level by gen_migration.py and lazily by _reimpl.py, and it names no private path of its own",
      "evidence": "fork F5M S4a (2026-09-16): re-classed from archive when the archive-class withhold rule was drawn -- a keep gate IMPORTS this module, so the public tree cannot run its own lint without it. Measured by import closure over the keep set; the publish-ledger closure arm cannot see this class of edge, because it scans path-form strings and `import _cstrip` has none."
    },
    "migration_ledger.py": {
      "class": "archive",
      "why": "migration ledger schema (state/evidence-tier vocabulary)",
      "evidence": "F0 triage (docs/fork-inventory.md section 1): the migration / promotion / ledger machinery the fork drops"
    },
    "migration_loop.py": {
      "class": "archive",
      "why": "unattended migration session-loop driver",
      "evidence": "F0 triage (docs/fork-inventory.md section 1): the migration / promotion / ledger machinery the fork drops"
    },
    "migration_ready.py": {
      "class": "archive",
      "why": "migration slice readiness gate",
      "evidence": "F0 triage (docs/fork-inventory.md section 1): the migration / promotion / ledger machinery the fork drops"
    },
    "migration_seeds.py": {
      "class": "archive",
      "why": "access-defined migration domain membership",
      "evidence": "F0 triage (docs/fork-inventory.md section 1): the migration / promotion / ledger machinery the fork drops"
    },
    "migration_session.py": {
      "class": "archive",
      "why": "migration session bookends (--start/--end)",
      "evidence": "F0 triage (docs/fork-inventory.md section 1): the migration / promotion / ledger machinery the fork drops"
    },
    "mp_analyze.py": {
      "class": "keep",
      "why": "MP run log analyzer / determinism verdict",
      "evidence": "invoked by tools/lint_repo.py"
    },
    "mp_desync_snap_diff.py": {
      "class": "keep",
      "why": "desync full-state snapshot byte-diff",
      "evidence": "no machine invoker -- run by a human holding two desync snapshots",
      "hand_run": true
    },
    "mp_order_diff.py": {
      "class": "keep",
      "why": "peer order-recording diff/decoder",
      "evidence": "used by rig_batch.py"
    },
    "mp_pacing_report.py": {
      "class": "keep",
      "why": "MP pacing metrics report",
      "evidence": "used by rig_batch.py"
    },
    "mp_prep_manual.py": {
      "class": "keep",
      "why": "preps peers for a manual (hand-clicked) MP test",
      "evidence": "no machine invoker -- run by a human about to hand-click a multiplayer test",
      "hand_run": true
    },
    "mp_run.py": {
      "class": "keep",
      "why": "one-command N-peer MP test runner",
      "evidence": "used by hostlock.py, mp_prep_manual.py, mp_run_sweep.py, mp_step_sweep.py, provision_rig.py, test_ui.py, ui_test.py"
    },
    "mp_run_sweep.py": {
      "class": "keep",
      "why": "MP pacing-knob combination sweep",
      "evidence": "no machine invoker -- run by a human sweeping the pacing knobs; the gate runs fixed settings; documented in .claude/skills/mh-toolchain/SKILL.md",
      "hand_run": true
    },
    "mp_step_sweep.py": {
      "class": "keep",
      "why": "sub-millisecond lockstep-step sweep",
      "evidence": "no machine invoker -- run by a human sweeping sub-millisecond step sizes",
      "hand_run": true
    },
    "mutate.py": {
      "class": "keep",
      "why": "mutation-testing engine for reimplementation modules",
      "evidence": "no machine invoker -- run by a human asking whether a module's selftests would notice a planted defect",
      "hand_run": true
    },
    "net_shim.py": {
      "class": "keep",
      "why": "controllable TCP middlebox for the 2-VM rig",
      "evidence": "used by ui_test.py"
    },
    "order_stream.py": {
      "class": "keep",
      "why": "hook-free per-step order-decision channel reader",
      "evidence": "no machine invoker -- run by a human reading the per-step order channel (migration_ab.py also imports it, but that is an archive-side caller and cannot carry the row)",
      "hand_run": true
    },
    "probe_global_widths.py": {
      "class": "archive",
      "why": "Ghidra global-width probe",
      "evidence": "F0 triage (docs/fork-inventory.md section 1): the Ghidra DB pipeline (the fork freezes the database)"
    },
    "proc.py": {
      "class": "keep",
      "why": "subprocess helpers with orphan-proof timeouts",
      "evidence": "used by mutate.py"
    },
    "prof_resolve.py": {
      "class": "keep",
      "why": "profiler RVA resolver (mh.dll half fine; mh.exe half needs docs/symbols.md). KEEP as a hand-run harness tool: the mh.dll half needs nothing from Ghidra; the mh.exe half degrades to unresolved addresses without docs/symbols.md",
      "evidence": "no machine invoker -- run by a human resolving a profiler dump",
      "hand_run": true
    },
    "propagate_crt.py": {
      "class": "archive",
      "why": "Ghidra CRT-tag propagation/rename",
      "evidence": "F0 triage (docs/fork-inventory.md section 1): the Ghidra DB pipeline (the fork freezes the database)"
    },
    "prove_suite_identity.py": {
      "class": "keep",
      "why": "per-suite transcript+exit-code identity baseline for the selftest exe -- the oracle F5I's move and exe split are measured against (fork F5I)",
      "evidence": "no machine invoker -- run by hand around a change claimed to be behaviour-neutral: --record on the known-good build, --check on the candidate",
      "hand_run": true
    },
    "provision_rig.py": {
      "class": "keep",
      "why": "brings a fresh/relocated machine to rig-bootable state",
      "evidence": "no machine invoker -- run by a human bringing a rig up (the bootstrap skill); documented in .claude/skills/bootstrap/SKILL.md",
      "hand_run": true
    },
    "rank_dig_targets.py": {
      "class": "archive",
      "why": "naming-pipeline dig-target ranker",
      "evidence": "F0 triage (docs/fork-inventory.md section 1): the naming/documentation pipeline (Ghidra-side, dropped with the DB)"
    },
    "record_dead_dispositions.py": {
      "class": "archive",
      "why": "migration-ledger dead-row disposition writer",
      "evidence": "F0 triage (docs/fork-inventory.md section 1): the migration / promotion / ledger machinery the fork drops"
    },
    "release_package.py": {
      "class": "keep",
      "why": "TL-CI1: the release packager -- assembles the three drop-in zips (net / net-debug / brokered-debug) plus SHA256SUMS from a built Release tree, and refuses a missing artifact, a version the binaries do not carry, or the standalone libmh.dll offered as the hosted one",
      "evidence": "invoked by tools/lint_repo.py (--selftest) and by .github/workflows/release.yml"
    },
    "replay_libref.py": {
      "class": "keep",
      "why": "the libref replay driver (a gate unit)",
      "evidence": "invoked by tools/lint_repo.py, tools/run_gate.py"
    },
    "replay_match_segment.py": {
      "class": "keep",
      "why": "mp:SES7b: replays a bug report's recorded match (the SES7 segment, or a first-match process recording) offline in a fresh process and names the first diverging step and region",
      "evidence": "invoked by tools/lint_repo.py"
    },
    "report_inbound_liveness.py": {
      "class": "keep",
      "why": "which libmh inbound entries a run actually entered. KEEP as a hand-run report: the run-side complement to gen_libmh_inbound's static gate (LIB-SPINE-API)",
      "evidence": "no machine invoker -- run by a human asking which inbound entries a finished run entered",
      "hand_run": true
    },
    "report_patch_surface.py": {
      "class": "archive",
      "why": "Ghidra-dependent patch-surface report",
      "evidence": "F0 triage (docs/fork-inventory.md section 1): the Ghidra DB pipeline (the fork freezes the database)"
    },
    "report_type_usage.py": {
      "class": "archive",
      "why": "Ghidra struct-type usage report",
      "evidence": "F0 triage (docs/fork-inventory.md section 1): the Ghidra DB pipeline (the fork freezes the database)"
    },
    "resolve_doc_refs.py": {
      "class": "archive",
      "why": "address-based doc symbol-reference resolver",
      "evidence": "F0 triage (docs/fork-inventory.md section 1): the research-doc + diary lints the fork does not ship"
    },
    "retype_split_globals.py": {
      "class": "archive",
      "why": "Ghidra split-double global retyper",
      "evidence": "F0 triage (docs/fork-inventory.md section 1): the Ghidra DB pipeline (the fork freezes the database)"
    },
    "reva_call.py": {
      "class": "archive",
      "why": "ReVA MCP HTTP transport CLI",
      "evidence": "F0 triage (docs/fork-inventory.md section 1): the Ghidra DB pipeline (the fork freezes the database)"
    },
    "reva_service.py": {
      "class": "archive",
      "why": "headless Ghidra+ReVA launcher",
      "evidence": "F0 triage (docs/fork-inventory.md section 1): the Ghidra DB pipeline (the fork freezes the database)"
    },
    "rig_batch.py": {
      "class": "keep",
      "why": "paired determinism-experiment batch runner",
      "evidence": "no machine invoker -- run by a human running a paired determinism experiment",
      "hand_run": true
    },
    "run_gate.py": {
      "class": "keep",
      "why": "overlapped DLL-gate driver (unit list needs pruning)",
      "evidence": "run by a human -- this IS the gate driver, the root of everything below it; documented in .claude/skills/ui-testing/SKILL.md -- and invoked by tools/lint_repo.py (--selftest, its cost red rules)"
    },
    "run_selftests.py": {
      "class": "keep",
      "why": "DLL selftest gate (ASan-by-default) driver",
      "evidence": "invoked by tools/run_gate.py"
    },
    "save_checkin.py": {
      "class": "archive",
      "why": "Ghidra save+checkin script",
      "evidence": "F0 triage (docs/fork-inventory.md section 1): the Ghidra DB pipeline (the fork freezes the database)"
    },
    "scan_asm_writes.py": {
      "class": "archive",
      "why": "migration-domain write-set re-derivation from .asm",
      "evidence": "F0 triage (docs/fork-inventory.md section 1): the migration / promotion / ledger machinery the fork drops"
    },
    "scan_dat_mismatches.py": {
      "class": "archive",
      "why": "Ghidra _DAT_ width-mismatch scan",
      "evidence": "F0 triage (docs/fork-inventory.md section 1): the Ghidra DB pipeline (the fork freezes the database)"
    },
    "scan_interior_ptrs.py": {
      "class": "keep",
      "why": "interior-pointer scan over the committed exports (relocation hazard)",
      "evidence": "invoked by tools/lint_repo.py"
    },
    "scan_libmh_vas.py": {
      "class": "keep",
      "why": "link-proof scan: no original VA survives in built libmh.lib",
      "evidence": "invoked by src/mh_dll/libmh/libmh.vcxproj, tools/lint_repo.py"
    },
    "scan_limit_refsites.py": {
      "class": "archive",
      "why": "Ghidra hardcoded-limit-array ref-site classifier",
      "evidence": "F0 triage (docs/fork-inventory.md section 1): the Ghidra DB pipeline (the fork freezes the database)"
    },
    "scan_pointer_slots.py": {
      "class": "keep",
      "why": "pointer-typed state-region relocation-hazard scan. KEEP: a lint_repo row; pointer-slot relocation hazard is a libmh hosting concern, not a research one",
      "evidence": "invoked by tools/lint_repo.py"
    },
    "scan_raw_pointers.py": {
      "class": "archive",
      "why": "Ghidra raw (untracked) pointer reverse-index scan",
      "evidence": "F0 triage (docs/fork-inventory.md section 1): the Ghidra DB pipeline (the fork freezes the database)"
    },
    "scan_undef_calls.py": {
      "class": "archive",
      "why": "Ghidra calls-into-undefined-bytes scan",
      "evidence": "F0 triage (docs/fork-inventory.md section 1): the Ghidra DB pipeline (the fork freezes the database)"
    },
    "scan_unnamed_globals.py": {
      "class": "archive",
      "why": "naming-pipeline unnamed-global scan",
      "evidence": "F0 triage (docs/fork-inventory.md section 1): the naming/documentation pipeline (Ghidra-side, dropped with the DB)"
    },
    "scan_untyped_structs.py": {
      "class": "archive",
      "why": "Ghidra untyped-struct-pointer survey",
      "evidence": "F0 triage (docs/fork-inventory.md section 1): the Ghidra DB pipeline (the fork freezes the database)"
    },
    "set_calling_convention.py": {
      "class": "archive",
      "why": "Ghidra calling-convention setter",
      "evidence": "F0 triage (docs/fork-inventory.md section 1): the Ghidra DB pipeline (the fork freezes the database)"
    },
    "setup_dat.py": {
      "class": "keep",
      "why": "edits the game's setup.dat MRU lists (rig client IP pinning)",
      "evidence": "used by ui_test.py"
    },
    "slice_asm.py": {
      "class": "archive",
      "why": ".asm slicer for migration-translation lint pairing",
      "evidence": "F0 triage (docs/fork-inventory.md section 1): the migration / promotion / ledger machinery the fork drops"
    },
    "soak_test.py": {
      "class": "keep",
      "why": "tooling:TL-SUITE-SPLIT: the all-AI soak split out of test_ui.py -- an N-way AI match through the real menu with run-shape rules and the per-step golden hash trajectory",
      "evidence": "invoked by tools/coverage.py (the collector's inner soak) and tools/provision_rig.py (--validate); test_ui.py forwards its old --soak flag here"
    },
    "sweep_saves.py": {
      "class": "archive",
      "why": "AI-state save indexer for migration scenario coverage",
      "evidence": "F0 triage (docs/fork-inventory.md section 1): the migration / promotion / ledger machinery the fork drops"
    },
    "t3g_lookahead_replay.py": {
      "class": "keep",
      "why": "mp:T3g: alternate-history replay of lookahead_decide (the pre-T3c 9b0d758a era and the current one, hand-ported) against a run's mh_lockstep.log -- a negative result (best 40% off the 10% bar: the log's row cadence cannot reconstruct the per-frame median + binding-peer OWD) kept for the next controller comparison",
      "evidence": "no machine invoker -- hand-run by an analyst comparing controller eras; cited by docs/mp-latency.md section 3.2c",
      "hand_run": true
    },
    "tact_divergence_hunt.py": {
      "class": "keep",
      "why": "tactical A/B divergence rate-measurement loop. KEEP as a hand-run instrument: it is what turns 'a run went green' into a rate with an N beside it for the tactical determinism blocker",
      "evidence": "no machine invoker -- run by a human measuring the tactical divergence rate over N runs",
      "hand_run": true
    },
    "tact_profile.py": {
      "class": "keep",
      "why": "differential rig wall-time profiler",
      "evidence": "no machine invoker -- run by a human profiling a differential rig run",
      "hand_run": true
    },
    "tact_test.py": {
      "class": "keep",
      "why": "tooling:TL-SUITE-SPLIT: the tactical-mode harness split out of test_ui.py -- journal equiv / verify / replay / one arm / determinism / play / trim / suite",
      "evidence": "imported by tools/test_ui.py (the pooled tactical-journal tail runs run_tact_equiv), tools/coverage.py, tools/tact_profile.py and tools/tact_divergence_hunt.py; test_ui.py forwards its old --tact-* flags here"
    },
    "temporal_unify.py": {
      "class": "keep",
      "why": "MP per-event temporal-trace unifier",
      "evidence": "no machine invoker -- run by a human unifying two peers' event traces after a desync; documented in .claude/skills/mh-toolchain/SKILL.md",
      "hand_run": true
    },
    "test_ui.py": {
      "class": "keep",
      "why": "UI regression suite driver: the registry (TESTS) runner + its offline lints; forwards the old mode flags to det_arms.py / tact_test.py / ui_abc.py / soak_test.py for one cycle (tooling:TL-SUITE-SPLIT)",
      "evidence": "invoked by tools/lint_repo.py, tools/run_gate.py"
    },
    "track.py": {
      "class": "archive",
      "why": "structured-tracker query/regenerate (trackers dropped)",
      "evidence": "F0 triage (docs/fork-inventory.md section 1): the migration / promotion / ledger machinery the fork drops"
    },
    "udp_rtt_probe.py": {
      "class": "keep",
      "why": "UDP echo + round-trip prober: what the rig path actually delivers, measured by something that is not the game",
      "evidence": "no machine invoker -- an operator deploys the echo half to a rig peer and runs the probe half through net_shim when a latency number has to be checked against the wire. Added at mp:T3b, which is the failure it exists to stop: the shim had been 'cleared' on a LOOPBACK echo while the DLL was measured on a two-VM path, so the two numbers were never compared on ONE path. `selftest` proves the instrument itself.",
      "hand_run": true
    },
    "ui_abc.py": {
      "class": "keep",
      "why": "tooling:TL-SUITE-SPLIT: the recorded-UI-session (UI-REC) modes split out of test_ui.py -- the A/B/C campaign oracle, equiv, replay, selftest, oracle cut, record",
      "evidence": "invoked by tools/run_gate.py (abc_spcamp, abc_tutorial units); imported by tools/coverage.py; test_ui.py forwards its old --ui-* flags here"
    },
    "ui_registry.py": {
      "class": "keep",
      "why": "TL-SUITE-REGDATA: loads + schema-checks tools/uiscripts/registry.yaml (the TESTS / sim / uirec / tact scenario registries); an unknown key, wrong type or missing key refuses the load by row name",
      "evidence": "imported by tools/test_ui.py (binds TESTS etc. at import), tools/coverage.py and tools/journal_coverage.py (registered_journals); its --selftest is a tools/lint_repo.py row"
    },
    "ui_suite_common.py": {
      "class": "keep",
      "why": "tooling:TL-SUITE-SPLIT: shared machinery of the UI-suite entry points -- scenario registries, the ONE ui_test.py argv builder (build_scenario_argv, TL-SUITE-ARGV), RunnerConfig (TL-SUITE-CONFIG), lane provisioning, the relay process, the shared flags",
      "evidence": "imported by tools/test_ui.py, tools/det_arms.py, tools/tact_test.py, tools/ui_abc.py, tools/soak_test.py, tools/coverage.py and tools/tact_profile.py"
    },
    "ui_test.py": {
      "class": "keep",
      "why": "UI regression runner (single- and multi-peer)",
      "evidence": "used by fixture_replay.py, rig_batch.py, test_ui.py"
    },
    "lint_rig_ini_defaults.py": {
      "class": "keep",
      "why": "tooling:TL-SUITE-INIMERGE: diffs ui_test.py's NET_BLOCK [net] defaults against the shipped src/mh_dll/mh_net.example.ini (same trap as TL-RIG-DEFANG/G274, where the rig silently ran defang_overlay=1 for two years against a shipped 0) and reds on an undeclared mismatch; declared ones live in tools/data/rig_ini_default_exceptions.json.",
      "evidence": "invoked by tools/lint_repo.py"
    },
    "win_job.py": {
      "class": "keep",
      "why": "tooling:TL-SUITE-TEARDOWN: the shared Windows Job-Object kill-on-close helper (JOB_OBJECT_LIMIT_KILL_ON_JOB_CLOSE) + process-liveness primitives, generalised from TL-RIG6's shim-only mechanism so every local child this rig owns (game peers, the isolated-desktop CreateProcessW path, the relay/relay-shim) dies with its launching Python process by ONE code path.",
      "evidence": "imported by tools/ui_test.py (peer launches, shim_start/shim_stop), tools/desktop.py (spawn), tools/test_ui.py (RelayProc, the relay-shim leg); its --selftest is a tools/lint_repo.py row"
    },
    "load_red_allow.py": {
      "class": "keep",
      "why": "tooling:TL-SUITE-LOADRED: validates tools/data/load_red_allow.json (the solo-rerun allow-list) -- refuses a malformed entry or one whose expiry has passed, so an accepted load-flaky row cannot silently stay accepted forever",
      "evidence": "invoked by tools/test_ui.py (apply_load_rerun's allow_check) and tools/lint_repo.py"
    },
    "lint_ui_sync.py": {
      "class": "keep",
      "why": "tooling:TL-SUITE-LOADRED (b): refuses a uiscript step or a registry shim_timeline row whose only synchronization is a clock offset/sleep instead of a state predicate, a shim_triggers log-line gate, or a simstep fence (TL-P9W-TRIGGER's fixed t+50s blackhole is the motivating, still-open offender)",
      "evidence": "invoked by tools/lint_repo.py"
    },
    "lint_shutdown_counters.py": {
      "class": "keep",
      "why": "tooling:TL-SUITE-COUNTERS (docs/dead-ends.md G101): refuses a g_* counter whose only log print is inside a shutdown-path function or message -- a rig run is always KILLED, so that print never runs. Validates tools/data/shutdown_counter_allow.json's own shape too.",
      "evidence": "invoked by tools/lint_repo.py"
    }
  }
}
