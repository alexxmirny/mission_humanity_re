; mp:CH1 -- appended to a peer's mh_net.ini by `ui_test.py --extra-ini` for the REPRODUCTION arm only.
;
; The shipping default is cheat_gate=1: in a network game the hidden single-player cheat console
; refuses everything but `_NETDELAY`. This fragment restores RETAIL behaviour so the ch1_cheat walk
; can show what the 2026-09-20 field report described -- a `_NUCLEAR BOMB` typed on the Shift+Enter
; line going out on the lockstep order stream and wiping the other player's base. Never ship it,
; never pin it on a suite row: the suite runs the gate ON.
[input]
cheat_gate=0
