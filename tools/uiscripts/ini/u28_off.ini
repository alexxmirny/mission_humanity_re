; U28 NEGATIVE ARM -- host-side only. Sends the legacy bare FLAG_START (no slot payload), so each peer
; builds Players[] from its OWN lobby slots: the pre-U28 configuration. With mp_*_inflight.txt this
; must DESYNC at step 1. Never ship this; it exists so the fix's absence is demonstrable from an ini
; rather than by reverting code.
[net]
start_slots=0
