; D13 "or higher": a resolution ABOVE the stock ~1024-tile ceiling -- 1600x1184 (the 1200 is snapped
; down to a 32px tile multiple) is 50 x 37 = 1850 tiles, which only works because the seam relocates the
; five per-tile arrays into a MAX_TILES block. This is the test that proves the relocation.
[video]
width=1600
height=1200
