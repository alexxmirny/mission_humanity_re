; AUTHORING AID ONLY (mp:D28) -- logs every new active screen + its widget list to mh_uidrive.log,
; (the `[uitest] dump_screens=1` authoring aid of the ui-testing skill). Not meant to ride a committed
; registry row (verbose); merge it in only while writing/debugging a new script.
[uitest]
dump_screens=1
