; U21 -- the POSITIVE arm: the same malformed broadcast, guard ON (the ship default).
;
; The control for devchange_probe_off.ini. Same message, same timing; the game must survive
; it and the script must run to COMPLETE. Without this arm the negative one only proves that
; posting the message crashes an unguarded build -- not that the guard is what saves it.
[compat]
devchange_guard=1
devchange_probe_ms=6000
