; mp:F3c -- the CLIENT that WILL NOT switch, for the codepage_refused scenario (appended by
; `ui_test.py --extra-ini-client`).
;
; `codepage_adopt=0` is how a player declares this peer keeps its own codepage whatever the host
; advertises. Against the 1252 host fixture the JOIN then carries 1251, the host refuses it by name,
; and F3c's other half is what this scenario asserts: the refusal REACHES the joiner -- a
; JOIN_REFUSED announce bounces it out of the lobby it seated itself in, and the browser it lands on
; names the reason ("Refused: codepage 1252/1251" -- the host's value first, then this peer's).
[input]
codepage=1251
codepage_adopt=0
