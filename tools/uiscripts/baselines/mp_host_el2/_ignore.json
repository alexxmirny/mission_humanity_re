{
  "_why": "mp:EL2. capture_el2_host_lobby.png renders this machine's own adapter address in the right-panel header (192.168.x.y here), which setup.dat pinning cannot reach -- it comes from the adapters, not from the saved MRU. Standard right-panel own-IP rect, kept tight to the band above the map-preview text (ui-testing skill).",
  "capture_el2_host_lobby.png": [[388, 46, 226, 48]]
}
