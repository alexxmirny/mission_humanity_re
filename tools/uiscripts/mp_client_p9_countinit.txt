# resync_countinit_proof's own copy of mp_client_p9_proof.txt (2026-09-24): identical up to the match, then it ends
# on the runner's `signal countinit_done` (a shim_trigger on the host's `force_resync FIRED #2`
# line) instead of `gameclock 30000`. The count climbs per stall-nag, i.e. per rendered frame at
# the horizon, so a fixed game-clock window held 3 fires alone but 1 under gate load. The row's
# `timeout` is the bound when the second fire never comes.
# mp:P9 (2/2) -- THE PROOF ROWS' client (joiner) side. See mp_host_p9_proof.txt: match_launch's
# join walk, one in-game capture at match_launch's own SIM gate, then the match runs to gameclock
# 30 s with one fix on (the row's [net] knobs), and both peers end on their own -- no blackhole,
# no wedge. This is the joiner: `[net] lockstep_step_ms=60` (the row's net_extra_client, G267).

# join the host by IP -> the joined lobby (see mp_client_join.txt for the flow)
settled value:110
clickv 110
present Player name
settled Ok
clickl Ok
present Internet server
settled Internet server
clickl Internet server
present Server address
settled Connect
clickl Connect
sessions 1
settled Join game
clickl Join game

# joined lobby: tell the host we are really here, then wait for its Start
present Network players
signal lobby
settled Cancel
absent Network players

# in the match: the one capture, at match_launch's own gate
gameclock 6000
capture m3_launched
log CLIENT is in the configuration-(1) match; running until the leader's second barrier fire

awaitsignal countinit_done
log CLIENT saw the countinit_done signal (leader FIRED #2)
end
